{
  "goal": "A static Rust binary that reads what an agent produced and refuses the growth that should not be there, deleted or weakened tests, skips, stubs, swallowed errors, secrets, guardrail edits, dependency-direction violations, in milliseconds, with zero tokens, every finding as SARIF 2.1.0. `weed check` judges a diff and may block; `weed scan` judges the tree and never blocks; `weed guard` is the law in git through hooks; `weed bite` proves a test fails without its change. For maintainers drowning in agent-authored pull requests who need a deterministic gate before they read, for teams running agents on several harnesses who want one law, and for the garden's own fleets through pleach.",
  "source": "docs/tend2",
  "nodes": [
    {
      "id": "bite",
      "work": {
        "prompt": "You are a worker in a build loop. Make the checks below pass, honestly.\nNobody reviews your prose, only the verifier's verdict counts, so put all effort into the work itself.\n\n## The goal\nThe one dynamic rule. Given `--test \"<command>\"` and a base, `weed bite` checks out the test commit alone on the base in a temp worktree and runs the command: it must fail. Then it applies the implementation and runs again: it must pass. A test that passes without the change is theater, reported at block level as B1 with the names of the tests that passed. Premise: pleach commits the test phase separately from the implementation, which its umbrella loop carries as a check.\n\n## Your environment\n- You are at the repository root of an isolated git worktree. Work here directly.\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- The loop file (docs/tend2/bite.tend2.html) is your full context, read it, including its narrative. It is READ-ONLY for you.\n\n## Checks to close\n- (code) on a fixture with a test commit and an implementation commit, bite passes when the test fails alone and passes with the implementation, and reports B1 at block level naming the passing tests when the test passes alone · evidence expected at: tests/bite.rs\n- (code) a test command that hangs is exit 3 with the timeout named, and the temp worktree is removed on every path · evidence expected at: tests/bite_exec.rs\n- (code) the proof-repo measurement in `docs/bite-2026-09.md` shows at least 90 percent of phased nodes yield a clean test commit, or the file records the kill and `bite` is absent from `weed --help` · evidence expected at: scripts/check/bite-kill.sh\n\n## Definition of done, verify yourself before finishing\nYou are done when this exact command exits 0 (it is the gate the conductor will run):\n\n    tend2 verify docs/tend2/bite.tend2.html --repo-root . --force --expect-payload c70e20addfd4 --runner 'bash scripts/check/run.sh {evidence}'\n\nRun it yourself. If it does not exit 0, you are not done. Do not declare success in prose that a command disagrees with.\n\n## Already tried (do not repeat)\n- 2026-09-05 the earlier thorn bed folded in as bite, made cheap by pleach's separate test commit; no hunk splitting (umbrella Tried, 2026-09-05)\n- 2026-09-05 the premise is pleach's, not ours: if pleach's phased commits do not exist yet when this loop is dispatched, the worker measures what exists, writes the count, and the kill path is the honest outcome\n- 2026-09-05 as for scan: `garden.json` and `SKILL.md` must gain `bite` when the face lands, or `tests/garden_manifest.rs` and `tests/skill_md.rs` refuse it\n\n## Rules\n- Write the failing test first where it does not exist; then make it pass.\n- You must not edit the loop file, check states belong to the verifier.\n- You must not weaken, skip, or mock the unit under test to force a green; mocked-unit evidence is detected and refused.\n- Solve the CLASS of problem the claim names, not the test's example inputs. Hardcoding fixture strings, phrases, or patterns from the check or its tests into source (lookup tables, regex lists, phrase maps) is refused work even when the test goes green, it is an answer key, not a product.\n- If the claim cannot be met generically with what you have, that is a real finding: write BLOCKED.md naming the missing capability (a model, a library, a redesign, a split of the check into honest tiers) instead of faking generality.\n- Production quality only: no stubs, no TODOs, no placeholder implementations; match the style of the surrounding code.\n- Probe/scratch files (toolchain checks, experiments) go in `.loop-scratch/` only, it is never collected; scratch files elsewhere pollute the delivery.\n- If you are genuinely blocked, write BLOCKED.md at the repo root, what you tried, what stopped you, what a fix needs. An honest BLOCKED.md is a good outcome; a faked green is the worst possible one."
      },
      "accept": {
        "smoke": "tend2 verify docs/tend2/bite.tend2.html --repo-root . --force --expect-payload c70e20addfd4 --runner 'bash scripts/check/run.sh {evidence}'",
        "audit": {
          "command": "tend2 verify docs/tend2/bite.tend2.html --repo-root . --force --expect-payload c70e20addfd4 --audit-egress --runner 'bash scripts/check/run.sh {evidence}'",
          "provider": "codex",
          "selfIntegrity": true
        }
      },
      "policy": {
        "timeoutMs": 5400000
      },
      "needs": [
        "calibration"
      ],
      "worker": {
        "provider": "claude",
        "model": "opus"
      }
    },
    {
      "id": "calibration-audit",
      "work": {
        "prompt": "You are a worker in a build loop. Make the checks below pass, honestly.\nNobody reviews your prose, only the verifier's verdict counts, so put all effort into the work itself.\n\n## The goal\nThe classification of blocked commits into true positive, acceptable and false positive is the number weed ships on, so it is not the builder's word. A second party on a different provider re-classifies a random sample of at least twenty blocked commits from `docs/calibration-2026-09.md` and at least twenty cases from the recall sample, blind to the first classification, and writes `docs/calibration-audit-2026-09.md`. Agreement at or above 90 percent on each sample stands; below it, the classification is untrusted and redone before anything ships.\n\n## Your environment\n- You are at the repository root of an isolated git worktree. Work here directly.\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- The loop file (docs/tend2/calibration-audit.tend2.html) is your full context, read it, including its narrative. It is READ-ONLY for you.\n\n## Checks to close\n- (code) `docs/calibration-audit-2026-09.md` carries an independent re-classification of a seeded random sample of at least twenty blocked commits and at least twenty recall cases, each with the auditor's verdict and one line of reasoning, and names the provider that produced it · evidence expected at: scripts/check/calibration-agreement.sh\n- (code) agreement between the audit and the original classification is at or above 90 percent on each sample, or the calibration file's first sentence says the classification is untrusted · evidence expected at: scripts/check/calibration-agreement.sh\n\n## Definition of done, verify yourself before finishing\nYou are done when this exact command exits 0 (it is the gate the conductor will run):\n\n    tend2 verify docs/tend2/calibration-audit.tend2.html --repo-root . --force --expect-payload 40774682f5e8 --runner 'bash scripts/check/run.sh {evidence}'\n\nRun it yourself. If it does not exit 0, you are not done. Do not declare success in prose that a command disagrees with.\n\n## Already tried (do not repeat)\n- 2026-09-05 shaped from cape-town's binding checks; the owner may take the auditor's seat instead of a second provider, and the script does not care which\n\n## Rules\n- Write the failing test first where it does not exist; then make it pass.\n- You must not edit the loop file, check states belong to the verifier.\n- You must not weaken, skip, or mock the unit under test to force a green; mocked-unit evidence is detected and refused.\n- Solve the CLASS of problem the claim names, not the test's example inputs. Hardcoding fixture strings, phrases, or patterns from the check or its tests into source (lookup tables, regex lists, phrase maps) is refused work even when the test goes green, it is an answer key, not a product.\n- If the claim cannot be met generically with what you have, that is a real finding: write BLOCKED.md naming the missing capability (a model, a library, a redesign, a split of the check into honest tiers) instead of faking generality.\n- Production quality only: no stubs, no TODOs, no placeholder implementations; match the style of the surrounding code.\n- Probe/scratch files (toolchain checks, experiments) go in `.loop-scratch/` only, it is never collected; scratch files elsewhere pollute the delivery.\n- If you are genuinely blocked, write BLOCKED.md at the repo root, what you tried, what stopped you, what a fix needs. An honest BLOCKED.md is a good outcome; a faked green is the worst possible one."
      },
      "accept": {
        "smoke": "tend2 verify docs/tend2/calibration-audit.tend2.html --repo-root . --force --expect-payload 40774682f5e8 --runner 'bash scripts/check/run.sh {evidence}'",
        "audit": {
          "command": "tend2 verify docs/tend2/calibration-audit.tend2.html --repo-root . --force --expect-payload 40774682f5e8 --audit-egress --runner 'bash scripts/check/run.sh {evidence}'",
          "provider": "claude",
          "selfIntegrity": true
        }
      },
      "policy": {
        "timeoutMs": 5400000
      },
      "needs": [
        "calibration",
        "recall"
      ],
      "worker": {
        "provider": "codex"
      }
    },
    {
      "id": "calibration",
      "work": {
        "prompt": "You are a worker in a build loop. Make the checks below pass, honestly.\nNobody reviews your prose, only the verifier's verdict counts, so put all effort into the work itself.\n\n## The goal\nBefore weed is allowed to block anyone, it runs over real history: `weed check --base <parent> --strict` on up to the last 200 commits of the default branch in tilth, pleach, tend2, copeca and umbel. `docs/calibration-2026-09.md` records per repo the commits judged, blocked and warned, and classifies every blocked commit as true positive, acceptable, or false positive. The ship bar is block-level false positives under two percent of the pooled commits across the five repos, with per-repo counts shown; a repo with fewer than 50 commits is reported, not judged alone. The kill bar is reaching that bar only by disabling T1, T2, T3 or S1; then the verdict is written in the file's first sentence and weed does not ship as a gate. The metric command `garden.json` names is this measurement.\n\n## Your environment\n- You are at the repository root of an isolated git worktree. Work here directly.\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- The loop file (docs/tend2/calibration.tend2.html) is your full context, read it, including its narrative. It is READ-ONLY for you.\n\n## Checks to close\n- (code) `docs/calibration/corpus.toml` names each repository by a fetchable source and a full tip sha and carries no path on this machine; `cargo xtask calibrate` and `cargo xtask suppressions` fetch each tip into scratch and judge the window that ends at it, so a commit added to a source after the pin does not change the report, proven on a history built for the purpose, and the evidence scripts read the scratch rather than a checkout · evidence expected at: scripts/check/corpus-pinned.sh\n- (code) the report's first sentence reads `weed ships as a gate, pending the independent re-grade:` until `docs/calibration-audit-2026-09.md` records agreement at or above 90 percent on each sample, and `weed ships as a gate:` after; the generator reads the audit file to decide, so the wording is never an edit, and `calibration-bar.sh` refuses the unqualified sentence while the audit is absent or under the bar · evidence expected at: scripts/check/calibration-bar.sh\n- (code) the report carries, beside the pooled block-level false-positive share, the count of C3 warnings on workflow files that C1 blocked in the first run, recomputed from the tables, so the C1 split's effect is measured rather than assumed · evidence expected at: scripts/check/calibration-bar.sh\n\n## Definition of done, verify yourself before finishing\nYou are done when this exact command exits 0 (it is the gate the conductor will run):\n\n    tend2 verify docs/tend2/calibration.tend2.html --repo-root . --force --expect-payload fac085385512 --runner 'bash scripts/check/run.sh {evidence}'\n\nRun it yourself. If it does not exit 0, you are not done. Do not declare success in prose that a command disagrees with.\n\n## Already tried (do not repeat)\n- 2026-09-05 repos follow the umbrella loop's list; the brief's rctrl is umbel's former name and the same repository\n- 2026-09-05 `xtask` over a script so the calibration runs the same core the binary ships, with no second implementation of the diff walk\n- 2026-09-05 bar refined on the channel by cape-town: the two percent applies to the pooled total across repos, with per-repo counts shown; a repo under 50 commits is reported, not judged alone\n- 2026-09-05 cape-town: track the suppression rate from the day guard is installed and report it beside precision; rising suppressions with flat true positives means the gate is gamed or mis-tuned. The recall section of the same file belongs to the recall loop, and the second party's classification to calibration-audit\n- 2026-09-05 cape-town's ruling on specimens: `weed.toml` may exclude paths only under `fixtures/adversarial/**` (and the specimen directories a repo names), every excluded path is still reported at note level in the SARIF so an exclusion is as visible as an allowance, and it is a list of directories, never a rule toggle. This is what lets `weed check` on weed's own tree report nothing at block level, and it lands with calibration\n- 2026-09-06 built by pleach node `calibration` (claude/opus) in 55 minutes with all four checks stamped, then lost at pleach's staging step over two scratch files (dogfood P8) and recovered by replaying every write and edit in the worker's transcript at the base commit; tend2 verify stamped all four again on the recovered tree before it was merged. The verdict: weed ships as a gate, 1.42 percent pooled block-level false positives over 635 commits in five repositories, 71 blocked, 7 true positives, 55 acceptable, 9 false positives, T1 T2 T3 S1 all at block level. The worker's own notes are in docs/dogfood/calibration.md\n- 2026-09-06 the corpus names the five repositories by local checkout path, which the loop's narrative asked for and AGENTS.md forbids: the report reproduces on this machine only, and it changes whenever any of those checkouts moves. Shaped above as a check that pins each repository by source and tip sha, and the loop now needs rules-block too, so the recalibration counts the three rule fixes calibration found. The specimen ruling is on rules-prod; the C1 friction (42 of 71 blocks are real workflow edits) is a product question put to cape-town\n- 2026-09-06 the generated report's headings carried an em dash that two evidence scripts and a suite matched on; the prose sweep took it out of the generator and its readers together, and the report was regenerated. The title reads `calibration: weed over real history`\n- 2026-09-06 cape-town: the verdict is provisional until the codex re-grade agrees at or above 90 percent on the sample; the first sentence says so until then, and the shipped number is measured on the shipped rules and the shipped corpus, so calibration runs again after rules-block lands with the corpus pinned by source and sha first. C1 splits (rules-block's Tried) and the next run reports the warns that used to be blocks beside the false positives. Both shaped as checks above\n\n## Rules\n- Write the failing test first where it does not exist; then make it pass.\n- You must not edit the loop file, check states belong to the verifier.\n- You must not weaken, skip, or mock the unit under test to force a green; mocked-unit evidence is detected and refused.\n- Solve the CLASS of problem the claim names, not the test's example inputs. Hardcoding fixture strings, phrases, or patterns from the check or its tests into source (lookup tables, regex lists, phrase maps) is refused work even when the test goes green, it is an answer key, not a product.\n- If the claim cannot be met generically with what you have, that is a real finding: write BLOCKED.md naming the missing capability (a model, a library, a redesign, a split of the check into honest tiers) instead of faking generality.\n- Production quality only: no stubs, no TODOs, no placeholder implementations; match the style of the surrounding code.\n- Probe/scratch files (toolchain checks, experiments) go in `.loop-scratch/` only, it is never collected; scratch files elsewhere pollute the delivery.\n- If you are genuinely blocked, write BLOCKED.md at the repo root, what you tried, what stopped you, what a fix needs. An honest BLOCKED.md is a good outcome; a faked green is the worst possible one."
      },
      "accept": {
        "smoke": "tend2 verify docs/tend2/calibration.tend2.html --repo-root . --force --expect-payload fac085385512 --runner 'bash scripts/check/run.sh {evidence}'",
        "audit": {
          "command": "tend2 verify docs/tend2/calibration.tend2.html --repo-root . --force --expect-payload fac085385512 --audit-egress --runner 'bash scripts/check/run.sh {evidence}'",
          "provider": "codex",
          "selfIntegrity": true
        }
      },
      "policy": {
        "timeoutMs": 5400000
      },
      "needs": [
        "rules-prod",
        "rules-block"
      ],
      "worker": {
        "provider": "claude",
        "model": "opus"
      }
    },
    {
      "id": "recall",
      "work": {
        "prompt": "You are a worker in a build loop. Make the checks below pass, honestly.\nNobody reviews your prose, only the verifier's verdict counts, so put all effort into the work itself.\n\n## The goal\nCalibration measures false blocks on real good commits; this loop measures the mirror. `cargo xtask mutate` takes real merged commits from the five garden repos and injects each anti-pattern mechanically, a test case deleted, a `.skip` added, an assertion dropped, a `TODO` body, a swallowed error, a secret, a workflow file edited, one mutation per case, per language, then runs `weed check --base <parent> --strict` and records whether the rule fired on exactly that. Recall for the block-level rules T1, T3, S1, X1, C1 and G1 is at or above 95 percent per language; warn-level rules are reported, aiming at 80. Both numbers sit in `docs/calibration-2026-09.md` beside precision, on real code.\n\n## Your environment\n- You are at the repository root of an isolated git worktree. Work here directly.\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- The loop file (docs/tend2/recall.tend2.html) is your full context, read it, including its narrative. It is READ-ONLY for you.\n\n## Checks to close\n- (code) `cargo xtask mutate` injects one anti-pattern per case for every check rule in each of ts, py, rs and go from the calibration corpus, runs weed on each, and writes the recall section of `docs/calibration-2026-09.md` with every miss named · evidence expected at: scripts/check/recall.sh\n- (code) recall for T1, T3, S1, X1, C1 and G1 is at or above 95 percent per language, and every warn-level rule's recall is reported · evidence expected at: scripts/check/recall-bar.sh\n\n## Definition of done, verify yourself before finishing\nYou are done when this exact command exits 0 (it is the gate the conductor will run):\n\n    tend2 verify docs/tend2/recall.tend2.html --repo-root . --force --expect-payload d131de247182 --runner 'bash scripts/check/run.sh {evidence}'\n\nRun it yourself. If it does not exit 0, you are not done. Do not declare success in prose that a command disagrees with.\n\n## Already tried (do not repeat)\n- 2026-09-05 shaped from cape-town's binding checks: precision alone lets a gate ship that fires on nothing\n- 2026-09-05 one mutation per case, never several: a hit must be attributable to one rule, and a miss to one site\n\n## Rules\n- Write the failing test first where it does not exist; then make it pass.\n- You must not edit the loop file, check states belong to the verifier.\n- You must not weaken, skip, or mock the unit under test to force a green; mocked-unit evidence is detected and refused.\n- Solve the CLASS of problem the claim names, not the test's example inputs. Hardcoding fixture strings, phrases, or patterns from the check or its tests into source (lookup tables, regex lists, phrase maps) is refused work even when the test goes green, it is an answer key, not a product.\n- If the claim cannot be met generically with what you have, that is a real finding: write BLOCKED.md naming the missing capability (a model, a library, a redesign, a split of the check into honest tiers) instead of faking generality.\n- Production quality only: no stubs, no TODOs, no placeholder implementations; match the style of the surrounding code.\n- Probe/scratch files (toolchain checks, experiments) go in `.loop-scratch/` only, it is never collected; scratch files elsewhere pollute the delivery.\n- If you are genuinely blocked, write BLOCKED.md at the repo root, what you tried, what stopped you, what a fix needs. An honest BLOCKED.md is a good outcome; a faked green is the worst possible one."
      },
      "accept": {
        "smoke": "tend2 verify docs/tend2/recall.tend2.html --repo-root . --force --expect-payload d131de247182 --runner 'bash scripts/check/run.sh {evidence}'",
        "audit": {
          "command": "tend2 verify docs/tend2/recall.tend2.html --repo-root . --force --expect-payload d131de247182 --audit-egress --runner 'bash scripts/check/run.sh {evidence}'",
          "provider": "codex",
          "selfIntegrity": true
        }
      },
      "policy": {
        "timeoutMs": 5400000
      },
      "needs": [
        "rules-prod"
      ],
      "worker": {
        "provider": "claude",
        "model": "opus"
      }
    },
    {
      "id": "rules-block",
      "work": {
        "prompt": "You are a worker in a build loop. Make the checks below pass, honestly.\nNobody reviews your prose, only the verifier's verdict counts, so put all effort into the work itself.\n\n## The goal\nThe rules that block by default, because each names a shape no honest change has: a deleted test, an added skip or focus, a stub in production code, a secret, an edited guardrail, and a conflict marker (G1, already live on check-face). Each lands as a failing fixture first in TypeScript/JavaScript, Python, Rust and Go, then the detector, and each has a neighbour fixture where it must stay silent. A rule detects a shape, never a vendor or a product name.\n\n## Your environment\n- You are at the repository root of an isolated git worktree. Work here directly.\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- The loop file (docs/tend2/rules-block.tend2.html) is your full context, read it, including its narrative. It is READ-ONLY for you.\n\n## Checks to close\n- (code) C1 fires at block level on the harness hook configs (`.claude/settings*.json`, `.gemini/settings.json`, `.codex/*`), on `.githooks/*` other than weed's own bundle, on `weed.toml`, and on a hunk touching the hard-limits section of `AGENTS.md` or `CLAUDE.md`; it stays silent on `.github/CODEOWNERS`, on a hunk elsewhere in `AGENTS.md`, and on a GitHub workflow file · evidence expected at: tests/rule_c1.rs\n- (code) X1 fires on a known-prefix token in any file and applies its key-like-name heuristic only to files classified as source code, so a `name = value` shape inside a markup attribute or a line of markdown prose about credentials stays silent while the same shape in ts, py, rs and go source still fires · evidence expected at: tests/rule_x1.rs\n- (code) S1 stays silent on a Python method whose body is an ellipsis inside a `Protocol` or an abstract class, where the ellipsis declares a type rather than defers an implementation, and still fires on an ellipsis body in a concrete function or class · evidence expected at: tests/rule_s1.rs\n- (code) T1 counts a test case generated in a loop or a table as present, so cases that move from static declarations into a `for` around `it`, a `t.Run` in a range, a `parametrize` list or a macro-generated `#[test]` are not reported as deleted, in ts, py, rs and go, while a case that leaves the file altogether still is · evidence expected at: tests/rule_t1.rs\n- (code) C3 warns by default on a change to a file under `.github/workflows/`, fire and silent fixtures alike, and a repository that names workflow paths under `[guardrails] paths` in `weed.toml` gets them at block level; the catalogue, `docs/rules.md` and `weed rules` carry C3 as a warn rule with what/why/next in its message · evidence expected at: tests/rule_c3.rs\n\n## Definition of done, verify yourself before finishing\nYou are done when this exact command exits 0 (it is the gate the conductor will run):\n\n    tend2 verify docs/tend2/rules-block.tend2.html --repo-root . --force --expect-payload b30fe9d88813 --runner 'bash scripts/check/run.sh {evidence}'\n\nRun it yourself. If it does not exit 0, you are not done. Do not declare success in prose that a command disagrees with.\n\n## Already tried (do not repeat)\n- 2026-09-05 rule files are `src/core/rules/check/<id>.rs`, one per rule, registered in `src/core/rules/check/mod.rs`; scan rules get their own registry so the two faces never share a file\n- 2026-09-05 T2 (assertions dropped) is on rules-tests although it blocks by default: its count depends on per-language assertion vocabularies that the tests loop owns, and the block set here is the one the umbrella loop names as unambiguous\n- 2026-09-05 X1's prefix list overlaps pleach's hygiene gate on purpose: both are the same shapes, and weed is the one that runs where pleach does not\n- 2026-09-05 built by pleach node `rules-block` (claude/opus, codex audit) in one attempt, 41 minutes, landed by hand after `pleach land` met the hardening loop's change to the git seam; merged as both readings (a NUL byte is a binary blob and reads as nothing, anything else reads as text with stray bytes replaced). T1 counts cases from the reader's test shape on both sides; T3, S1 and X1 read through a syntax mask so a marker inside a string or a comment says nothing; the secret and marker fixtures use placeholders the harness expands, so the repository carries no credential and no marker\n- 2026-09-05 conductor correction, fixed inline and re-verified: the worker concluded that weed must refuse its own hook installation under C1 (`.githooks/*` is a guardrail path) and made adoption a commit that skips the hooks, which the hooks face denies outright. C1 now recognises weed's own bundle byte for byte (`guard::is_own_bundle`: the binary its marker names and the branches its exec line protects, handed back to `guard::script`), so adopting weed goes through the gate the hooks run, and a bundle that differs by one byte is a guardrail edit again. `tests/rule_c1.rs` pins both halves; the guard tests adopt weed with ordinary commits\n- 2026-09-05 left open, for calibration: `weed check` on weed's own tree reports the T3 and S1 fixtures, which are meant to look dishonest. A per-path specimen exclusion in `weed.toml` is the fix; a fixture that stops carrying a skip stops being a fixture\n- 2026-09-05 cape-town on C1's self-recognition: byte for byte on weed's own bundle is the right shape; when the stem later generates that bundle, the pin moves to the stem's output. Never widen it to a path match\n- 2026-09-06 calibration over 635 real commits (docs/calibration-2026-09.md, `Where weed was wrong`) found three shapes worth their own checks, added above: seven of nine block-level false positives are X1 reading `name = value` in a css class attribute or a markdown sentence; S1 calls a Python `Protocol` method with an ellipsis body a stub; T1 counts only statically declared cases, so a case generated in a loop reads as one that vanished. Calibration now needs this loop, so the next run measures the fixed rules\n- 2026-09-06 cape-town's C1 ruling, after calibration showed 42 of 71 blocks were honest workflow edits: split it. Block-level C1 is the constitution tier, the agent's own guardrails, harness hook configs, `.githooks/*`, `weed.toml`, the hard-limits sections of `AGENTS.md` and `CLAUDE.md`. GitHub workflow files are by-laws, edited legitimately all the time, and move to C3 at warn, promoted to block per repository under `[guardrails] paths`. The C1 check above is reworded to the split, which voids its stamp on purpose\n\n## Rules\n- Write the failing test first where it does not exist; then make it pass.\n- You must not edit the loop file, check states belong to the verifier.\n- You must not weaken, skip, or mock the unit under test to force a green; mocked-unit evidence is detected and refused.\n- Solve the CLASS of problem the claim names, not the test's example inputs. Hardcoding fixture strings, phrases, or patterns from the check or its tests into source (lookup tables, regex lists, phrase maps) is refused work even when the test goes green, it is an answer key, not a product.\n- If the claim cannot be met generically with what you have, that is a real finding: write BLOCKED.md naming the missing capability (a model, a library, a redesign, a split of the check into honest tiers) instead of faking generality.\n- Production quality only: no stubs, no TODOs, no placeholder implementations; match the style of the surrounding code.\n- Probe/scratch files (toolchain checks, experiments) go in `.loop-scratch/` only, it is never collected; scratch files elsewhere pollute the delivery.\n- If you are genuinely blocked, write BLOCKED.md at the repo root, what you tried, what stopped you, what a fix needs. An honest BLOCKED.md is a good outcome; a faked green is the worst possible one."
      },
      "accept": {
        "smoke": "tend2 verify docs/tend2/rules-block.tend2.html --repo-root . --force --expect-payload b30fe9d88813 --runner 'bash scripts/check/run.sh {evidence}'",
        "audit": {
          "command": "tend2 verify docs/tend2/rules-block.tend2.html --repo-root . --force --expect-payload b30fe9d88813 --audit-egress --runner 'bash scripts/check/run.sh {evidence}'",
          "provider": "codex",
          "selfIntegrity": true
        }
      },
      "policy": {
        "timeoutMs": 5400000
      },
      "worker": {
        "provider": "claude",
        "model": "opus"
      }
    },
    {
      "id": "rules-prod",
      "work": {
        "prompt": "You are a worker in a build loop. Make the checks below pass, honestly.\nNobody reviews your prose, only the verifier's verdict counts, so put all effort into the work itself.\n\n## The goal\nThe rest of the check catalogue: swallowed errors, debug leftovers, dependency manifest changes, dependency-direction violations, out-of-scope files, a broadened ignore, and a large or binary file. D2 and X2 block when configured; the rest warn. Each lands as a failing fixture first in TypeScript/JavaScript, Python, Rust and Go, with a silent neighbour.\n\n## Your environment\n- You are at the repository root of an isolated git worktree. Work here directly.\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- The loop file (docs/tend2/rules-prod.tend2.html) is your full context, read it, including its narrative. It is READ-ONLY for you.\n\n## Checks to close\n- (code) `[scope] specimens` in `weed.toml` is a list of directories under `fixtures/adversarial/**` that `check` and `scan` skip for every rule, each skipped path reported once at note level so an exclusion is as visible as an allowance; an entry outside that root, a glob that reaches past it, or a rule name in the list is refused with exit 3 and a message naming it · evidence expected at: tests/config_specimens.rs\n\n## Definition of done, verify yourself before finishing\nYou are done when this exact command exits 0 (it is the gate the conductor will run):\n\n    tend2 verify docs/tend2/rules-prod.tend2.html --repo-root . --force --expect-payload e7abb0eaece0 --runner 'bash scripts/check/run.sh {evidence}'\n\nRun it yourself. If it does not exit 0, you are not done. Do not declare success in prose that a command disagrees with.\n\n## Already tried (do not repeat)\n- 2026-09-05 the catalogue is complete after this loop: eighteen check rules with G1 on check-face; `docs/rules.md` is written here and `weed rules` reads from the same table, so the two cannot drift\n- 2026-09-05 S2's Go shape is deliberately narrow (`return nil` after a nil check with nothing else in the block) because Go's idiom makes wider shapes noisy; calibration decides whether it stays a warning\n- 2026-09-06 built by pleach node `rules-prod` (claude/opus, codex audit) in one attempt, 65 minutes; the land gate refused the composition with scan (both landings added a `comments` view to the mask, and scan still read the tree as text where rules-prod now reads a blob), resolved by hand. Every detector is a pure function of one `Judgement` the face gathers once: changes, config, scope, tracked paths, callers. D1 warns and blocks when a scope excludes the manifest, and the registry keeps a level a detector chose above its default; D2 resolves a module name by matching its segments against the repository's own paths and refuses to guess between two layers; X2 names the blast radius through the reader's callers; C2 turns an ignore pattern into globs and reports what it hides; G2 reads size and binariness off the same bytes; S3 reads `[entrypoints] cli`; the vendor name the brief listed became the shape `dump`. weed.toml now states weed's own layers, so D2 holds weed to its doctrine\n- 2026-09-06 the catalogue is complete: eighteen check rules and four scan rules, `docs/rules.md` written from the same table and pinned to it by `tests/docs_rules.rs`, so every SARIF helpUri lands somewhere\n- 2026-09-06 left for calibration: D2 reads every import of a changed file, so touching a file that already reaches the wrong way blocks the change that touched it. Right for a clean tree, wrong for a repository adopting the rule with a mess; whether existing arrows need an allowance is calibration's call. S2's Go shape is narrow on purpose, and calibration decides if it is still too wide\n- 2026-09-06 cape-town's specimen ruling (calibration's Tried) lands here as a check rather than in calibration: it is a config surface, and the calibration worker rightly refused to move the numbers it was measuring by changing a detector's input mid-run\n\n## Rules\n- Write the failing test first where it does not exist; then make it pass.\n- You must not edit the loop file, check states belong to the verifier.\n- You must not weaken, skip, or mock the unit under test to force a green; mocked-unit evidence is detected and refused.\n- Solve the CLASS of problem the claim names, not the test's example inputs. Hardcoding fixture strings, phrases, or patterns from the check or its tests into source (lookup tables, regex lists, phrase maps) is refused work even when the test goes green, it is an answer key, not a product.\n- If the claim cannot be met generically with what you have, that is a real finding: write BLOCKED.md naming the missing capability (a model, a library, a redesign, a split of the check into honest tiers) instead of faking generality.\n- Production quality only: no stubs, no TODOs, no placeholder implementations; match the style of the surrounding code.\n- Probe/scratch files (toolchain checks, experiments) go in `.loop-scratch/` only, it is never collected; scratch files elsewhere pollute the delivery.\n- If you are genuinely blocked, write BLOCKED.md at the repo root, what you tried, what stopped you, what a fix needs. An honest BLOCKED.md is a good outcome; a faked green is the worst possible one."
      },
      "accept": {
        "smoke": "tend2 verify docs/tend2/rules-prod.tend2.html --repo-root . --force --expect-payload e7abb0eaece0 --runner 'bash scripts/check/run.sh {evidence}'",
        "audit": {
          "command": "tend2 verify docs/tend2/rules-prod.tend2.html --repo-root . --force --expect-payload e7abb0eaece0 --audit-egress --runner 'bash scripts/check/run.sh {evidence}'",
          "provider": "codex",
          "selfIntegrity": true
        }
      },
      "policy": {
        "timeoutMs": 5400000
      },
      "worker": {
        "provider": "claude",
        "model": "opus"
      }
    },
    {
      "id": "weed",
      "work": {
        "prompt": "You are a worker in a build loop. Make the checks below pass, honestly.\nNobody reviews your prose, only the verifier's verdict counts, so put all effort into the work itself.\n\n## The goal\nA static Rust binary that reads what an agent produced and refuses the growth that should not be there, deleted or weakened tests, skips, stubs, swallowed errors, secrets, guardrail edits, dependency-direction violations, in milliseconds, with zero tokens, every finding as SARIF 2.1.0. `weed check` judges a diff and may block; `weed scan` judges the tree and never blocks; `weed guard` is the law in git through hooks; `weed bite` proves a test fails without its change. For maintainers drowning in agent-authored pull requests who need a deterministic gate before they read, for teams running agents on several harnesses who want one law, and for the garden's own fleets through pleach.\n\n## Your environment\n- You are at the repository root of an isolated git worktree. Work here directly.\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- The loop file (docs/tend2/weed.tend2.html) is your full context, read it, including its narrative. It is READ-ONLY for you.\n\n## Checks to close\n- (code) every rule the catalogue prints (`weed rules`) has a fire and a silent fixture for each of ts, py, rs and go under `fixtures/adversarial/`, and one test walks that matrix, replays each fixture through the real binary and asserts the expected result set · evidence expected at: tests/catalogue_matrix.rs\n- (code) `weed check --base <root-commit> --format sarif` on weed's own tree reports nothing at block level, and the log validates against the vendored SARIF schema · evidence expected at: tests/self_check.rs\n- (code) `weed check --staged` finishes in under 200 milliseconds wall-clock on a typical fixture diff and under 2 seconds on the worst case (a 50-file diff, a 20 MiB file), measured on the release binary, and the CI workflow runs this test so a regression fails the build · evidence expected at: tests/speed.rs\n\n## Definition of done, verify yourself before finishing\nYou are done when this exact command exits 0 (it is the gate the conductor will run):\n\n    tend2 verify docs/tend2/weed.tend2.html --repo-root . --force --expect-payload 16e19bb5343a --runner 'bash scripts/check/run.sh {evidence}'\n\nRun it yourself. If it does not exit 0, you are not done. Do not declare success in prose that a command disagrees with.\n\n## Already tried (do not repeat)\n- 2026-09-05 map shaped from the umbrella's fit loop and the build brief (revised 2026-09-05), before any code, as the garden law requires; the umbrella map holds fit (F1 to F6), this map holds modules\n- 2026-09-05 weed lives in its own repository, not the tilth Cargo workspace: cape-town's correction over the channel and the brief's revision agree; `building-the-garden.md` §5 and §8 still say \"in the tilth workspace\" and are reported stale to the umbrella rather than followed\n- 2026-09-05 process requirement from the owner: build with tend2 loops, pleach conducting, umbel driving, and record every experience with those tools in `docs/dogfood.md`. The brief's \"no subagents\" line yields to that: workers are pleach nodes, this map is their work order, and the conductor audits the result against the diff\n- 2026-09-05 `tilth-core` does not exist; the extraction is conducted first as its own pleach plan against a worktree of tilth (`plans/tilth-core.plan.json`), then this map's plan is emitted. Until the branch is pushed, `.cargo/config.toml` patches the git dependency to the local worktree, the one machine-specific line in the repo, flagged above\n- 2026-09-05 `--strict` defined, since the brief uses it without saying what it does: suppressions are reported but not honoured, and a rule that cannot evaluate a file makes the run fail closed (exit 3) instead of skipping the file. A gate an agent can talk out of a block with an allowance it wrote itself is not a gate\n- 2026-09-05 fixtures are directory pairs (`before/` committed, `after/` as the working tree), not patch files, so a fixture reads as code in its language and the test replays it through the real git path\n- 2026-09-05 the check rules are split across three sequential loops (block, tests, prod) rather than parallel siblings: they share the rule registry file and parallel edits there would merge-conflict on every join; guard, hooks and scan run in parallel with rules-tests since they own disjoint files\n- 2026-09-05 calibration repos follow the fit loop (tilth, pleach, tend2, copeca, umbel) rather than the brief's list with rctrl; several have fewer than 200 commits on their default branch, so the run takes every commit available and reports the count\n- 2026-09-05 scope-out: no rules that name vendors or products; no inbound injection quarantine; no spend rules (settled on the umbrella loop, not re-argued here)\n- 2026-09-05 `--strict` accepted by the umbrella with one refinement recorded in the brief: strict never escalates warn-level results to block; severity is the rule's, not the flag's\n- 2026-09-05 with core, sarif and check-face landed and the reader seam waiting on almaty, guard, hooks and garden-fit were re-pointed to run on G1 rather than wait for rules-block; the rule loops and scan still wait for the seam\n- 2026-09-05 milestone 1 met by almaty: tilth-core exists on `garden/tilth-core` at 7f38db5, pushed, so weed pins a rev and carries no machine-specific line; the push the owner was asked for is done by the tilth agent's PR\n- 2026-09-05 cape-town added six binding checks on the umbrella loop (determinism, recall by mutation injection, never panic, a latency budget, independent classification, suppression-rate tracking); they landed here as the hardening, recall and calibration-audit loops, a suppression-rate check on calibration, the latency budget above, and a CI clause on garden-fit\n- 2026-09-05 the owner settled all three reserved decisions through cape-town on the channel: the name weed, the accent bramble `#8E3B5E`, the licence MIT. The human check is approved on that word; the only owner-only actions left are on GitHub (publish, Pages), and the owner asked for the branch to be pushed and a draft pull request opened, which is done (#1)\n\n## Rules\n- Write the failing test first where it does not exist; then make it pass.\n- You must not edit the loop file, check states belong to the verifier.\n- You must not weaken, skip, or mock the unit under test to force a green; mocked-unit evidence is detected and refused.\n- Solve the CLASS of problem the claim names, not the test's example inputs. Hardcoding fixture strings, phrases, or patterns from the check or its tests into source (lookup tables, regex lists, phrase maps) is refused work even when the test goes green, it is an answer key, not a product.\n- If the claim cannot be met generically with what you have, that is a real finding: write BLOCKED.md naming the missing capability (a model, a library, a redesign, a split of the check into honest tiers) instead of faking generality.\n- Production quality only: no stubs, no TODOs, no placeholder implementations; match the style of the surrounding code.\n- Probe/scratch files (toolchain checks, experiments) go in `.loop-scratch/` only, it is never collected; scratch files elsewhere pollute the delivery.\n- If you are genuinely blocked, write BLOCKED.md at the repo root, what you tried, what stopped you, what a fix needs. An honest BLOCKED.md is a good outcome; a faked green is the worst possible one."
      },
      "accept": {
        "smoke": "tend2 verify docs/tend2/weed.tend2.html --repo-root . --force --expect-payload 16e19bb5343a --runner 'bash scripts/check/run.sh {evidence}'",
        "audit": {
          "command": "tend2 verify docs/tend2/weed.tend2.html --repo-root . --force --expect-payload 16e19bb5343a --audit-egress --runner 'bash scripts/check/run.sh {evidence}'",
          "provider": "codex",
          "selfIntegrity": true
        }
      },
      "policy": {
        "timeoutMs": 5400000
      },
      "needs": [
        "rules-block",
        "rules-prod",
        "calibration",
        "recall",
        "calibration-audit",
        "bite"
      ],
      "worker": {
        "provider": "claude",
        "model": "opus"
      }
    }
  ],
  "warnings": [
    {
      "loopId": "calibration",
      "code": "unwired-modules",
      "message": "7 checks cite 4 src modules (docs/calibration-2026-09.md, garden.json, scripts/check/calibration-bar.sh, docs/calibration/corpus.toml), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "core",
      "code": "unwired-modules",
      "message": "5 checks cite 5 src modules (Cargo.toml, weed.toml, AGENTS.md, CLAUDE.md, src/core/), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "garden-fit",
      "code": "unwired-modules",
      "message": "7 checks cite 13 src modules (Cargo.toml, schemas/garden.schema.json, .brand/products/weed/identity.md, .github/workflows/ci.yml, .github/workflows/release.yml, npm/package.json, npm/, garden.json, scripts/check/calibration-bar.sh, SKILL.md, .brand/products/weed/, examples/ci/github.yml, README.md), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "hardening",
      "code": "unwired-modules",
      "message": "5 checks cite 6 src modules (tests/determinism.rs, fixtures/adversarial/, tests/sarif_order.rs, src/core/, tests/core_hostile.rs, tests/check_hostile.rs), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "hooks",
      "code": "unwired-modules",
      "message": "4 checks cite 3 src modules (docs/proof-2026-09.md, scripts/proof/claude-stop.sh, scripts/proof/gemini-stop.sh), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "rules-block",
      "code": "unwired-modules",
      "message": "10 checks cite 5 src modules (weed.toml, AGENTS.md, CLAUDE.md, .github/workflows/, docs/rules.md), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "rules-prod",
      "code": "unwired-modules",
      "message": "8 checks cite 3 src modules (Cargo.toml, .gitignore, weed.toml), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "sarif",
      "code": "unwired-modules",
      "message": "4 checks cite 2 src modules (schemas/sarif-schema-2.1.0.json, docs/sarif.md), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "scan",
      "code": "unwired-modules",
      "message": "8 checks cite 5 src modules (CLAUDE.md, AGENTS.md, README.md, /, Cargo.toml), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "tilth-core",
      "code": "unwired-modules",
      "message": "4 checks cite 5 src modules (.context/tilth-core, src/seams/reader.rs, Cargo.toml, src/, src/core/), but no single check's evidence spans 2+ of them"
    }
  ]
}
