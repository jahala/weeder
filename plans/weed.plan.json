{
  "goal": "A static Rust binary that reads what an agent produced and refuses the growth that should not be there — deleted or weakened tests, skips, stubs, swallowed errors, secrets, guardrail edits, dependency-direction violations — in milliseconds, with zero tokens, every finding as SARIF 2.1.0. `weed check` judges a diff and may block; `weed scan` judges the tree and never blocks; `weed guard` is the law in git through hooks; `weed bite` proves a test fails without its change. For maintainers drowning in agent-authored pull requests who need a deterministic gate before they read, for teams running agents on several harnesses who want one law, and for the garden's own fleets through pleach.",
  "source": "docs/tend2",
  "nodes": [
    {
      "id": "bite",
      "work": {
        "prompt": "You are a worker in a build loop. Make the checks below pass, honestly.\nNobody reviews your prose — only the verifier's verdict counts, so put all effort into the work itself.\n\n## The goal\nThe one dynamic rule. Given `--test \"<command>\"` and a base, `weed bite` checks out the test commit alone on the base in a temp worktree and runs the command: it must fail. Then it applies the implementation and runs again: it must pass. A test that passes without the change is theater, reported at block level as B1 with the names of the tests that passed. Premise: pleach commits the test phase separately from the implementation, which its umbrella loop carries as a check.\n\n## Your environment\n- You are at the repository root of an isolated git worktree. Work here directly.\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- The loop file (docs/tend2/bite.tend2.html) is your full context — read it, including its narrative. It is READ-ONLY for you.\n\n## Checks to close\n- (code) on a fixture with a test commit and an implementation commit, bite passes when the test fails alone and passes with the implementation, and reports B1 at block level naming the passing tests when the test passes alone · evidence expected at: tests/bite.rs\n- (code) a test command that hangs is exit 3 with the timeout named, and the temp worktree is removed on every path · evidence expected at: tests/bite_exec.rs\n- (code) the proof-repo measurement in `docs/bite-2026-09.md` shows at least 90 percent of phased nodes yield a clean test commit, or the file records the kill and `bite` is absent from `weed --help` · evidence expected at: scripts/check/bite-kill.sh\n\n## Definition of done — verify yourself before finishing\nYou are done when this exact command exits 0 (it is the gate the conductor will run):\n\n    tend2 verify docs/tend2/bite.tend2.html --repo-root . --force --expect-payload af456bdb6208 --runner 'bash scripts/check/run.sh {evidence}'\n\nRun it yourself. If it does not exit 0, you are not done. Do not declare success in prose that a command disagrees with.\n\n## Already tried (do not repeat)\n- 2026-09-05 the earlier thorn bed folded in as bite, made cheap by pleach's separate test commit; no hunk splitting (umbrella Tried, 2026-09-05)\n- 2026-09-05 the premise is pleach's, not ours: if pleach's phased commits do not exist yet when this loop is dispatched, the worker measures what exists, writes the count, and the kill path is the honest outcome\n\n## Rules\n- Write the failing test first where it does not exist; then make it pass.\n- You must not edit the loop file — check states belong to the verifier.\n- You must not weaken, skip, or mock the unit under test to force a green; mocked-unit evidence is detected and refused.\n- Solve the CLASS of problem the claim names, not the test's example inputs. Hardcoding fixture strings, phrases, or patterns from the check or its tests into source (lookup tables, regex lists, phrase maps) is refused work even when the test goes green — it is an answer key, not a product.\n- If the claim cannot be met generically with what you have, that is a real finding: write BLOCKED.md naming the missing capability (a model, a library, a redesign, a split of the check into honest tiers) instead of faking generality.\n- Production quality only: no stubs, no TODOs, no placeholder implementations; match the style of the surrounding code.\n- Probe/scratch files (toolchain checks, experiments) go in `.loop-scratch/` only — it is never collected; scratch files elsewhere pollute the delivery.\n- If you are genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs. An honest BLOCKED.md is a good outcome; a faked green is the worst possible one."
      },
      "accept": {
        "smoke": "tend2 verify docs/tend2/bite.tend2.html --repo-root . --force --expect-payload af456bdb6208 --runner 'bash scripts/check/run.sh {evidence}'",
        "audit": {
          "command": "tend2 verify docs/tend2/bite.tend2.html --repo-root . --force --expect-payload af456bdb6208 --audit-egress",
          "provider": "codex",
          "selfIntegrity": true
        }
      },
      "policy": {
        "timeoutMs": 5400000
      },
      "needs": [
        "calibration"
      ],
      "worker": {
        "provider": "claude",
        "model": "opus"
      }
    },
    {
      "id": "calibration",
      "work": {
        "prompt": "You are a worker in a build loop. Make the checks below pass, honestly.\nNobody reviews your prose — only the verifier's verdict counts, so put all effort into the work itself.\n\n## The goal\nBefore weed is allowed to block anyone, it runs over real history: `weed check --base <parent> --strict` on up to the last 200 commits of the default branch in tilth, pleach, tend2, copeca and umbel. `docs/calibration-2026-09.md` records per repo the commits judged, blocked and warned, and classifies every blocked commit as true positive, acceptable, or false positive. The ship bar is block-level false positives under two percent of commits per repo. The kill bar is reaching that bar only by disabling T1, T2, T3 or S1; then the verdict is written in the file's first sentence and weed does not ship as a gate. The metric command `garden.json` names is this measurement.\n\n## Your environment\n- You are at the repository root of an isolated git worktree. Work here directly.\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- The loop file (docs/tend2/calibration.tend2.html) is your full context — read it, including its narrative. It is READ-ONLY for you.\n\n## Checks to close\n- (code) `cargo xtask calibrate` judges up to the last 200 commits of each listed repo against their parents, writes `docs/calibration-2026-09.md` in the described shape with the verdict first, and leaves the source repositories untouched · evidence expected at: scripts/check/calibrate.sh\n- (code) the calibration file's per-repo block-level false-positive share is under two percent of commits judged, and no line of it records T1, T2, T3 or S1 turned off to get there · evidence expected at: scripts/check/calibration-bar.sh\n- (code) `garden.json`'s metric command runs `scripts/check/calibration-bar.sh` and its latest result is committed · evidence expected at: scripts/check/metric.sh\n\n## Definition of done — verify yourself before finishing\nYou are done when this exact command exits 0 (it is the gate the conductor will run):\n\n    tend2 verify docs/tend2/calibration.tend2.html --repo-root . --force --expect-payload 56fdeaacdb3f --runner 'bash scripts/check/run.sh {evidence}'\n\nRun it yourself. If it does not exit 0, you are not done. Do not declare success in prose that a command disagrees with.\n\n## Already tried (do not repeat)\n- 2026-09-05 repos follow the umbrella loop's list; the brief's rctrl is umbel's former name and the same repository\n- 2026-09-05 `xtask` over a script so the calibration runs the same core the binary ships, with no second implementation of the diff walk\n\n## Rules\n- Write the failing test first where it does not exist; then make it pass.\n- You must not edit the loop file — check states belong to the verifier.\n- You must not weaken, skip, or mock the unit under test to force a green; mocked-unit evidence is detected and refused.\n- Solve the CLASS of problem the claim names, not the test's example inputs. Hardcoding fixture strings, phrases, or patterns from the check or its tests into source (lookup tables, regex lists, phrase maps) is refused work even when the test goes green — it is an answer key, not a product.\n- If the claim cannot be met generically with what you have, that is a real finding: write BLOCKED.md naming the missing capability (a model, a library, a redesign, a split of the check into honest tiers) instead of faking generality.\n- Production quality only: no stubs, no TODOs, no placeholder implementations; match the style of the surrounding code.\n- Probe/scratch files (toolchain checks, experiments) go in `.loop-scratch/` only — it is never collected; scratch files elsewhere pollute the delivery.\n- If you are genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs. An honest BLOCKED.md is a good outcome; a faked green is the worst possible one."
      },
      "accept": {
        "smoke": "tend2 verify docs/tend2/calibration.tend2.html --repo-root . --force --expect-payload 56fdeaacdb3f --runner 'bash scripts/check/run.sh {evidence}'",
        "audit": {
          "command": "tend2 verify docs/tend2/calibration.tend2.html --repo-root . --force --expect-payload 56fdeaacdb3f --audit-egress",
          "provider": "codex",
          "selfIntegrity": true
        }
      },
      "policy": {
        "timeoutMs": 5400000
      },
      "needs": [
        "rules-prod"
      ],
      "worker": {
        "provider": "claude",
        "model": "opus"
      }
    },
    {
      "id": "check-face",
      "work": {
        "prompt": "You are a worker in a build loop. Make the checks below pass, honestly.\nNobody reviews your prose — only the verifier's verdict counts, so put all effort into the work itself.\n\n## The goal\n`weed check` is the primary face: it judges the index plus the working tree against HEAD by default (what pleach's smoke gate sees after it stages a worker's files), `--staged` judges the index alone (the pre-commit face), `--base <ref>` judges the tree against a ref (CI). It prints SARIF when stdout is not a terminal and a table when it is, `--format` overrides, and it exits 0, 2 or 3 as the contract says. `weed rules` prints the catalogue. The git seam lives here, and the first rule runs end to end: G1, conflict markers.\n\n## Your environment\n- You are at the repository root of an isolated git worktree. Work here directly.\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- The loop file (docs/tend2/check-face.tend2.html) is your full context — read it, including its narrative. It is READ-ONLY for you.\n\n## Checks to close\n- (code) with no flags weed judges index plus working tree against HEAD, `--staged` judges the index only, and `--base <ref>` judges the tree against that ref, proven on a temp repo carrying a distinct change in each zone · evidence expected at: tests/check_scopes.rs\n- (code) exit 0 on a clean diff and on warnings only, 2 with at least one block-level result, 3 outside a git repository, on an unknown ref, and on a malformed `weed.toml`, each with one stderr line naming the cause · evidence expected at: tests/check_exit.rs\n- (code) SARIF on stdout when it is not a terminal, a table when it is, and `--format` overrides both · evidence expected at: tests/check_format.rs\n- (code) G1 fires at block level on conflict markers at line start (`<<<<<<<`, `=======`, `>>>>>>>`) in each of ts, py, rs and go through the real binary, and stays silent on a neighbour that carries `=======` inside a string and a markdown table rule · evidence expected at: tests/rule_g1.rs\n- (code) `weed rules` prints every rule with id, default level, face and one-line finding, and the levels match `Config::default()` · evidence expected at: tests/rules_catalogue.rs\n- (code) `--strict` reports a suppressed block finding at its original level and exits 2, while without `--strict` the same diff exits 0 with the finding as a note · evidence expected at: tests/check_strict.rs\n- (code) `examples/pleach/plan.json` declares `\"accept\": { \"smoke\": \"weed check --strict\" }` and passes `pleach validate`; `docs/pleach.md` explains that weed sees the staged files plus the tree against HEAD, so no base argument is needed · evidence expected at: scripts/check/pleach-example.sh\n\n## Definition of done — verify yourself before finishing\nYou are done when this exact command exits 0 (it is the gate the conductor will run):\n\n    tend2 verify docs/tend2/check-face.tend2.html --repo-root . --force --expect-payload fb4fbe592559 --runner 'bash scripts/check/run.sh {evidence}'\n\nRun it yourself. If it does not exit 0, you are not done. Do not declare success in prose that a command disagrees with.\n\n## Already tried (do not repeat)\n- 2026-09-05 default mode chosen from pleach's own gate: `accept.smoke` runs in the node worktree after scoped staging, exit-code gated, no arguments passed (`~/conductor/workspaces/pleach-v1/cayenne/src/loop/run-node.ts`, the smoke block)\n- 2026-09-05 G1 lands here, not in rules-block, so the pipeline is proven end to end by the simplest rule before the catalogue is built on it\n- 2026-09-05 the CLI is `clap` with derive; subcommands for scan, guard, bite and hook are added by their own loops, each in its own file under `src/faces/`\n\n## Rules\n- Write the failing test first where it does not exist; then make it pass.\n- You must not edit the loop file — check states belong to the verifier.\n- You must not weaken, skip, or mock the unit under test to force a green; mocked-unit evidence is detected and refused.\n- Solve the CLASS of problem the claim names, not the test's example inputs. Hardcoding fixture strings, phrases, or patterns from the check or its tests into source (lookup tables, regex lists, phrase maps) is refused work even when the test goes green — it is an answer key, not a product.\n- If the claim cannot be met generically with what you have, that is a real finding: write BLOCKED.md naming the missing capability (a model, a library, a redesign, a split of the check into honest tiers) instead of faking generality.\n- Production quality only: no stubs, no TODOs, no placeholder implementations; match the style of the surrounding code.\n- Probe/scratch files (toolchain checks, experiments) go in `.loop-scratch/` only — it is never collected; scratch files elsewhere pollute the delivery.\n- If you are genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs. An honest BLOCKED.md is a good outcome; a faked green is the worst possible one."
      },
      "accept": {
        "smoke": "tend2 verify docs/tend2/check-face.tend2.html --repo-root . --force --expect-payload fb4fbe592559 --runner 'bash scripts/check/run.sh {evidence}'",
        "audit": {
          "command": "tend2 verify docs/tend2/check-face.tend2.html --repo-root . --force --expect-payload fb4fbe592559 --audit-egress",
          "provider": "codex",
          "selfIntegrity": true
        }
      },
      "policy": {
        "timeoutMs": 5400000
      },
      "needs": [
        "core",
        "sarif"
      ],
      "worker": {
        "provider": "claude",
        "model": "opus"
      }
    },
    {
      "id": "core",
      "work": {
        "prompt": "You are a worker in a build loop. Make the checks below pass, honestly.\nNobody reviews your prose — only the verifier's verdict counts, so put all effort into the work itself.\n\n## The goal\nA crate whose core does no I/O and never panics on input: `parse_diff` turns unified diff text into hunks with old and new line numbers; `classify_file` names a path's kind and language; `Finding` is the one shape every rule produces; `weed.toml` parses into a typed config with defaults that ship in the binary; suppressions parse from commit trailers and inline comments. Everything a rule needs is a value handed in, so a rule is a function from data to findings.\n\n## Your environment\n- You are at the repository root of an isolated git worktree. Work here directly.\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- The loop file (docs/tend2/core.tend2.html) is your full context — read it, including its narrative. It is READ-ONLY for you.\n\n## Checks to close\n- (code) `parse_diff` reproduces the files, change kinds, hunks and per-line old/new numbers of captured `git diff` outputs covering adds, deletes, modifications, renames with edits, binary files, mode-only changes, several hunks, and the no-newline marker · evidence expected at: tests/core_diff.rs\n- (code) `classify_file` names kind and language for the four v1 languages and every guardrail, manifest and generated path the catalogue lists, and returns `Other` rather than guessing on anything else · evidence expected at: tests/core_classify.rs\n- (code) `weed.toml` parses rule levels, scope globs, dependency directions, thresholds and protected branches; an unknown key, an invalid level, or a scan rule given `block` is an error naming the key; an absent file yields the defaults · evidence expected at: tests/core_config.rs\n- (code) suppressions parse from a commit message trailer and from an inline comment, require a reason, and turn a matching finding into a note that carries the reason and the original level · evidence expected at: tests/core_suppress.rs\n- (code) `src/core/` imports neither `std::fs`, `std::process`, `std::env`, `git2`, nor anything from `crate::seams` or `crate::faces`, checked by a test that reads the source tree · evidence expected at: tests/core_purity.rs\n\n## Definition of done — verify yourself before finishing\nYou are done when this exact command exits 0 (it is the gate the conductor will run):\n\n    tend2 verify docs/tend2/core.tend2.html --repo-root . --force --expect-payload 25183d537229 --runner 'bash scripts/check/run.sh {evidence}'\n\nRun it yourself. If it does not exit 0, you are not done. Do not declare success in prose that a command disagrees with.\n\n## Already tried (do not repeat)\n- 2026-09-05 this loop creates the crate: `Cargo.toml` (package `weed`, bin `weed`, edition 2021, release profile copied from tilth: opt-level 3, fat lto, one codegen unit, strip, panic abort), `src/lib.rs`, `src/main.rs` printing version and usage, `src/core/`; `tests/common/mod.rs` with the fixture harness other loops reuse (temp git repo from `before/`, working tree from `after/`, run the built binary, parse SARIF)\n- 2026-09-05 chose our own diff parser over shelling to a library: the input is always git's own unified format and the parser is a few hundred lines that the tests pin exactly\n- 2026-09-05 chose `git2` for the seams later, not here; core never sees a repository\n\n## Rules\n- Write the failing test first where it does not exist; then make it pass.\n- You must not edit the loop file — check states belong to the verifier.\n- You must not weaken, skip, or mock the unit under test to force a green; mocked-unit evidence is detected and refused.\n- Solve the CLASS of problem the claim names, not the test's example inputs. Hardcoding fixture strings, phrases, or patterns from the check or its tests into source (lookup tables, regex lists, phrase maps) is refused work even when the test goes green — it is an answer key, not a product.\n- If the claim cannot be met generically with what you have, that is a real finding: write BLOCKED.md naming the missing capability (a model, a library, a redesign, a split of the check into honest tiers) instead of faking generality.\n- Production quality only: no stubs, no TODOs, no placeholder implementations; match the style of the surrounding code.\n- Probe/scratch files (toolchain checks, experiments) go in `.loop-scratch/` only — it is never collected; scratch files elsewhere pollute the delivery.\n- If you are genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs. An honest BLOCKED.md is a good outcome; a faked green is the worst possible one."
      },
      "accept": {
        "smoke": "tend2 verify docs/tend2/core.tend2.html --repo-root . --force --expect-payload 25183d537229 --runner 'bash scripts/check/run.sh {evidence}'",
        "audit": {
          "command": "tend2 verify docs/tend2/core.tend2.html --repo-root . --force --expect-payload 25183d537229 --audit-egress",
          "provider": "codex",
          "selfIntegrity": true
        }
      },
      "policy": {
        "timeoutMs": 5400000
      },
      "worker": {
        "provider": "claude",
        "model": "opus"
      }
    },
    {
      "id": "garden-fit",
      "work": {
        "prompt": "You are a worker in a build loop. Make the checks below pass, honestly.\nNobody reviews your prose — only the verifier's verdict counts, so put all effort into the work itself.\n\n## The goal\nThe umbrella verifies every bed against a fit contract, F1 to F6. This loop ships what those checks read: `garden.json` at the root; a `SKILL.md` with a one-sentence description; the brand layer at `.brand/products/weed/` with the name and accent marked flagged; a `LICENSE` (MIT, flagged until the owner confirms); a `README.md`; the release pipeline copied from tilth's (CI, multi-platform binaries, crates.io, an npm wrapper); and the GitHub Action example that is weed's first distribution, `examples/ci/github.yml`.\n\n## Your environment\n- You are at the repository root of an isolated git worktree. Work here directly.\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- The loop file (docs/tend2/garden-fit.tend2.html) is your full context — read it, including its narrative. It is READ-ONLY for you.\n\n## Checks to close\n- (code) `garden.json` validates against `schemas/garden.schema.json`, its version equals `Cargo.toml`'s, its check command runs and emits valid SARIF, its metric command runs, and its declared context cost is 0 · evidence expected at: tests/garden_manifest.rs\n- (code) `SKILL.md` has a one-sentence description and its body names every subcommand and flag `weed --help` and each `weed <face> --help` print · evidence expected at: tests/skill_md.rs\n- (code) `.brand/products/weed/` holds identity, colors, voice, a petalsrc example and assets in the pollen shape, three accent candidates with measured contrast ratios, and `[flagged]` on the name and the accent · evidence expected at: scripts/check/brand.sh\n- (code) both workflows pass `actionlint`, `release.yml` carries the version check and the platform matrix, and `npm/package.json` names the same version as `Cargo.toml` · evidence expected at: scripts/check/release.sh\n- (code) `examples/ci/github.yml` runs `weed check --base origin/${{ github.base_ref }} --strict --format sarif` into `weed.sarif` and uploads it with `github/codeql-action/upload-sarif`, and passes `actionlint` · evidence expected at: scripts/check/ci-example.sh\n- (code) `LICENSE` is MIT naming the owner, `README.md` opens with what weed is and marks the license, name and accent as flagged, and `README.md` cites only commands and paths that exist · evidence expected at: scripts/check/readme.sh\n- (code) a fresh clone of this repository into a temp dir builds with `cargo build --release` and `weed --version` runs, on this machine (F3 as far as it can be proven before the tilth-core branch is pushed) · evidence expected at: scripts/check/clean-install.sh\n\n## Definition of done — verify yourself before finishing\nYou are done when this exact command exits 0 (it is the gate the conductor will run):\n\n    tend2 verify docs/tend2/garden-fit.tend2.html --repo-root . --force --expect-payload 0eb4c478620a --runner 'bash scripts/check/run.sh {evidence}'\n\nRun it yourself. If it does not exit 0, you are not done. Do not declare success in prose that a command disagrees with.\n\n## Already tried (do not repeat)\n- 2026-09-05 the fit runner `scripts/fit/weed.sh` and `garden.lock` on the umbrella do not exist yet; this loop ships what they will read and the umbrella loop stays open until they do\n- 2026-09-05 a landing page is a later loop; the garden footer with the plotplot band goes on the README until a page exists\n\n## Rules\n- Write the failing test first where it does not exist; then make it pass.\n- You must not edit the loop file — check states belong to the verifier.\n- You must not weaken, skip, or mock the unit under test to force a green; mocked-unit evidence is detected and refused.\n- Solve the CLASS of problem the claim names, not the test's example inputs. Hardcoding fixture strings, phrases, or patterns from the check or its tests into source (lookup tables, regex lists, phrase maps) is refused work even when the test goes green — it is an answer key, not a product.\n- If the claim cannot be met generically with what you have, that is a real finding: write BLOCKED.md naming the missing capability (a model, a library, a redesign, a split of the check into honest tiers) instead of faking generality.\n- Production quality only: no stubs, no TODOs, no placeholder implementations; match the style of the surrounding code.\n- Probe/scratch files (toolchain checks, experiments) go in `.loop-scratch/` only — it is never collected; scratch files elsewhere pollute the delivery.\n- If you are genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs. An honest BLOCKED.md is a good outcome; a faked green is the worst possible one."
      },
      "accept": {
        "smoke": "tend2 verify docs/tend2/garden-fit.tend2.html --repo-root . --force --expect-payload 0eb4c478620a --runner 'bash scripts/check/run.sh {evidence}'",
        "audit": {
          "command": "tend2 verify docs/tend2/garden-fit.tend2.html --repo-root . --force --expect-payload 0eb4c478620a --audit-egress",
          "provider": "codex",
          "selfIntegrity": true
        }
      },
      "policy": {
        "timeoutMs": 5400000
      },
      "needs": [
        "calibration",
        "check-face"
      ],
      "worker": {
        "provider": "claude",
        "model": "opus"
      }
    },
    {
      "id": "guard",
      "work": {
        "prompt": "You are a worker in a build loop. Make the checks below pass, honestly.\nNobody reviews your prose — only the verifier's verdict counts, so put all effort into the work itself.\n\n## The goal\n`weed guard install` puts weed's judgement where no harness can route around it: git hooks through `core.hooksPath`. Pre-commit runs `weed check --staged --strict`; pre-push runs `weed check --strict --base <upstream>` and refuses a non-fast-forward push to a protected branch; pre-rebase refuses rewriting protected history. `guard status` proves each hook is live; `guard uninstall` restores what was there. The only bypass, `--no-verify`, is denied by the harness hook (the hooks loop) and by the bundle the stem generates.\n\n## Your environment\n- You are at the repository root of an isolated git worktree. Work here directly.\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- The loop file (docs/tend2/guard.tend2.html) is your full context — read it, including its narrative. It is READ-ONLY for you.\n\n## Checks to close\n- (code) `install` writes the three hooks, sets `core.hooksPath`, and records the previous value; `status` reports each hook live; `uninstall` restores the previous `core.hooksPath` and removes only the files it wrote · evidence expected at: tests/guard_install.rs\n- (code) in a fixture repo the installed pre-commit refuses a commit that deletes a test, prints the findings, and allows a clean commit · evidence expected at: tests/guard_commit.rs\n- (code) against a real bare remote the installed pre-push refuses a non-fast-forward push to a protected branch and a push whose range carries a block-level finding, and allows a clean fast-forward · evidence expected at: tests/guard_push.rs\n- (code) the installed pre-rebase refuses rebasing a protected branch and allows rebasing a feature branch · evidence expected at: tests/guard_rebase.rs\n- (code) `status` exits 2 and names the miss when a hook is missing, not executable, or `core.hooksPath` points elsewhere · evidence expected at: tests/guard_status.rs\n\n## Definition of done — verify yourself before finishing\nYou are done when this exact command exits 0 (it is the gate the conductor will run):\n\n    tend2 verify docs/tend2/guard.tend2.html --repo-root . --force --expect-payload 2da745be73b3 --runner 'bash scripts/check/run.sh {evidence}'\n\nRun it yourself. If it does not exit 0, you are not done. Do not declare success in prose that a command disagrees with.\n\n## Already tried (do not repeat)\n- 2026-09-05 hooks are shell scripts calling the binary rather than copies of the binary: a copied binary goes stale on upgrade, a script that names a path fails loudly\n- 2026-09-05 the earlier hedge bed's deny list of shell patterns was folded into this loop and the harness hooks (umbrella Tried, 2026-09-05): a pattern list loses to `bash -c`, git hooks do not\n\n## Rules\n- Write the failing test first where it does not exist; then make it pass.\n- You must not edit the loop file — check states belong to the verifier.\n- You must not weaken, skip, or mock the unit under test to force a green; mocked-unit evidence is detected and refused.\n- Solve the CLASS of problem the claim names, not the test's example inputs. Hardcoding fixture strings, phrases, or patterns from the check or its tests into source (lookup tables, regex lists, phrase maps) is refused work even when the test goes green — it is an answer key, not a product.\n- If the claim cannot be met generically with what you have, that is a real finding: write BLOCKED.md naming the missing capability (a model, a library, a redesign, a split of the check into honest tiers) instead of faking generality.\n- Production quality only: no stubs, no TODOs, no placeholder implementations; match the style of the surrounding code.\n- Probe/scratch files (toolchain checks, experiments) go in `.loop-scratch/` only — it is never collected; scratch files elsewhere pollute the delivery.\n- If you are genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs. An honest BLOCKED.md is a good outcome; a faked green is the worst possible one."
      },
      "accept": {
        "smoke": "tend2 verify docs/tend2/guard.tend2.html --repo-root . --force --expect-payload 2da745be73b3 --runner 'bash scripts/check/run.sh {evidence}'",
        "audit": {
          "command": "tend2 verify docs/tend2/guard.tend2.html --repo-root . --force --expect-payload 2da745be73b3 --audit-egress",
          "provider": "codex",
          "selfIntegrity": true
        }
      },
      "policy": {
        "timeoutMs": 5400000
      },
      "needs": [
        "rules-block"
      ],
      "worker": {
        "provider": "claude",
        "model": "opus"
      }
    },
    {
      "id": "hooks",
      "work": {
        "prompt": "You are a worker in a build loop. Make the checks below pass, honestly.\nNobody reviews your prose — only the verifier's verdict counts, so put all effort into the work itself.\n\n## The goal\n`weed hook <claude|gemini|codex>` reads a harness hook event as JSON on stdin and answers in the harness's own shape. For a tool call that runs `git commit`, it judges the index and denies with the findings as the reason; for the end of a turn (Stop on Claude Code, AfterAgent on Gemini CLI), it judges the working tree and blocks the stop with the findings, so an agent cannot declare done over a dishonest tree. Codex gets whatever its hook events can deny, and the rest is written down. Every hook is proven with a real harness run recorded in `docs/proof-2026-09.md`.\n\n## Your environment\n- You are at the repository root of an isolated git worktree. Work here directly.\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- The loop file (docs/tend2/hooks.tend2.html) is your full context — read it, including its narrative. It is READ-ONLY for you.\n\n## Checks to close\n- (code) `weed hook claude` denies a `PreToolUse` Bash event whose command runs `git commit` over a red index with the table as the reason, denies `--no-verify` outright, blocks a `Stop` over a red tree and allows it over a clean one, and passes every other event through · evidence expected at: tests/hook_claude.rs\n- (code) `weed hook gemini` handles `BeforeTool` and `AfterAgent` the same way in Gemini's JSON shapes · evidence expected at: tests/hook_gemini.rs\n- (code) `weed hook codex` implements the events Codex can deny and prints a one-line note for the ones it cannot; the note matches the section in `docs/proof-2026-09.md` · evidence expected at: tests/hook_codex.rs\n- (code) a real Claude Code run with the Stop hook refuses a premature done with weed's findings as the reason and allows it once the tree is clean, captured into `docs/proof-2026-09.md` · evidence expected at: scripts/proof/claude-stop.sh\n- (code) a real Gemini CLI run with the AfterAgent hook does the same, captured into `docs/proof-2026-09.md` · evidence expected at: scripts/proof/gemini-stop.sh\n\n## Definition of done — verify yourself before finishing\nYou are done when this exact command exits 0 (it is the gate the conductor will run):\n\n    tend2 verify docs/tend2/hooks.tend2.html --repo-root . --force --expect-payload 795c46fd018a --runner 'bash scripts/check/run.sh {evidence}'\n\nRun it yourself. If it does not exit 0, you are not done. Do not declare success in prose that a command disagrees with.\n\n## Already tried (do not repeat)\n- 2026-09-05 the harness hook is the second line, not the first: `guard` is the law in git; the hook denies the bypass (`--no-verify`) and blocks a premature stop, which git hooks cannot see\n- 2026-09-05 the proof scripts run real harness sessions and are cited as evidence rather than folded into `cargo test`, so the Rust suite stays runnable on a machine with no agent CLI\n\n## Rules\n- Write the failing test first where it does not exist; then make it pass.\n- You must not edit the loop file — check states belong to the verifier.\n- You must not weaken, skip, or mock the unit under test to force a green; mocked-unit evidence is detected and refused.\n- Solve the CLASS of problem the claim names, not the test's example inputs. Hardcoding fixture strings, phrases, or patterns from the check or its tests into source (lookup tables, regex lists, phrase maps) is refused work even when the test goes green — it is an answer key, not a product.\n- If the claim cannot be met generically with what you have, that is a real finding: write BLOCKED.md naming the missing capability (a model, a library, a redesign, a split of the check into honest tiers) instead of faking generality.\n- Production quality only: no stubs, no TODOs, no placeholder implementations; match the style of the surrounding code.\n- Probe/scratch files (toolchain checks, experiments) go in `.loop-scratch/` only — it is never collected; scratch files elsewhere pollute the delivery.\n- If you are genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs. An honest BLOCKED.md is a good outcome; a faked green is the worst possible one."
      },
      "accept": {
        "smoke": "tend2 verify docs/tend2/hooks.tend2.html --repo-root . --force --expect-payload 795c46fd018a --runner 'bash scripts/check/run.sh {evidence}'",
        "audit": {
          "command": "tend2 verify docs/tend2/hooks.tend2.html --repo-root . --force --expect-payload 795c46fd018a --audit-egress",
          "provider": "codex",
          "selfIntegrity": true
        }
      },
      "policy": {
        "timeoutMs": 5400000
      },
      "needs": [
        "rules-block"
      ],
      "worker": {
        "provider": "claude",
        "model": "opus"
      }
    },
    {
      "id": "rules-block",
      "work": {
        "prompt": "You are a worker in a build loop. Make the checks below pass, honestly.\nNobody reviews your prose — only the verifier's verdict counts, so put all effort into the work itself.\n\n## The goal\nThe rules that block by default, because each names a shape no honest change has: a deleted test, an added skip or focus, a stub in production code, a secret, an edited guardrail, and a conflict marker (G1, already live on check-face). Each lands as a failing fixture first in TypeScript/JavaScript, Python, Rust and Go, then the detector, and each has a neighbour fixture where it must stay silent. A rule detects a shape, never a vendor or a product name.\n\n## Your environment\n- You are at the repository root of an isolated git worktree. Work here directly.\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- The loop file (docs/tend2/rules-block.tend2.html) is your full context — read it, including its narrative. It is READ-ONLY for you.\n\n## Checks to close\n- (code) T1 fires at block level when a test file is deleted and when a test case is removed from a changed test file, in ts, py, rs and go, and stays silent when a case is renamed in place or a new case is added · evidence expected at: tests/rule_t1.rs\n- (code) T3 fires at block level on each added skip or focus form per language and stays silent when the same token sits inside a string or a comment · evidence expected at: tests/rule_t3.rs\n- (code) S1 fires at block level on each stub form per language in production files, including the body-only shapes, and stays silent on the same tokens in test files and on a `TODO` inside a string literal · evidence expected at: tests/rule_s1.rs\n- (code) X1 fires at block level on each known prefix and on a high-entropy literal assigned to a key-like name, and stays silent on a short name, on a placeholder like `<your-key-here>`, and on a hash in a lockfile · evidence expected at: tests/rule_x1.rs\n- (code) C1 fires at block level on every listed guardrail path and on a hunk touching the hard-limits section of `AGENTS.md`, and stays silent on `.github/CODEOWNERS` and on a hunk elsewhere in `AGENTS.md` · evidence expected at: tests/rule_c1.rs\n- (code) every block rule's result carries a what/why/next message and, for T3 and G1, a `fixes` entry that removes the added line · evidence expected at: tests/rule_messages.rs\n\n## Definition of done — verify yourself before finishing\nYou are done when this exact command exits 0 (it is the gate the conductor will run):\n\n    tend2 verify docs/tend2/rules-block.tend2.html --repo-root . --force --expect-payload d488c34b86d9 --runner 'bash scripts/check/run.sh {evidence}'\n\nRun it yourself. If it does not exit 0, you are not done. Do not declare success in prose that a command disagrees with.\n\n## Already tried (do not repeat)\n- 2026-09-05 rule files are `src/core/rules/check/<id>.rs`, one per rule, registered in `src/core/rules/check/mod.rs`; scan rules get their own registry so the two faces never share a file\n- 2026-09-05 T2 (assertions dropped) is on rules-tests although it blocks by default: its count depends on per-language assertion vocabularies that the tests loop owns, and the block set here is the one the umbrella loop names as unambiguous\n- 2026-09-05 X1's prefix list overlaps pleach's hygiene gate on purpose: both are the same shapes, and weed is the one that runs where pleach does not\n\n## Rules\n- Write the failing test first where it does not exist; then make it pass.\n- You must not edit the loop file — check states belong to the verifier.\n- You must not weaken, skip, or mock the unit under test to force a green; mocked-unit evidence is detected and refused.\n- Solve the CLASS of problem the claim names, not the test's example inputs. Hardcoding fixture strings, phrases, or patterns from the check or its tests into source (lookup tables, regex lists, phrase maps) is refused work even when the test goes green — it is an answer key, not a product.\n- If the claim cannot be met generically with what you have, that is a real finding: write BLOCKED.md naming the missing capability (a model, a library, a redesign, a split of the check into honest tiers) instead of faking generality.\n- Production quality only: no stubs, no TODOs, no placeholder implementations; match the style of the surrounding code.\n- Probe/scratch files (toolchain checks, experiments) go in `.loop-scratch/` only — it is never collected; scratch files elsewhere pollute the delivery.\n- If you are genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs. An honest BLOCKED.md is a good outcome; a faked green is the worst possible one."
      },
      "accept": {
        "smoke": "tend2 verify docs/tend2/rules-block.tend2.html --repo-root . --force --expect-payload d488c34b86d9 --runner 'bash scripts/check/run.sh {evidence}'",
        "audit": {
          "command": "tend2 verify docs/tend2/rules-block.tend2.html --repo-root . --force --expect-payload d488c34b86d9 --audit-egress",
          "provider": "codex",
          "selfIntegrity": true
        }
      },
      "policy": {
        "timeoutMs": 5400000
      },
      "needs": [
        "check-face",
        "tilth-core"
      ],
      "worker": {
        "provider": "claude",
        "model": "opus"
      }
    },
    {
      "id": "rules-prod",
      "work": {
        "prompt": "You are a worker in a build loop. Make the checks below pass, honestly.\nNobody reviews your prose — only the verifier's verdict counts, so put all effort into the work itself.\n\n## The goal\nThe rest of the check catalogue: swallowed errors, debug leftovers, dependency manifest changes, dependency-direction violations, out-of-scope files, a broadened ignore, and a large or binary file. D2 and X2 block when configured; the rest warn. Each lands as a failing fixture first in TypeScript/JavaScript, Python, Rust and Go, with a silent neighbour.\n\n## Your environment\n- You are at the repository root of an isolated git worktree. Work here directly.\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- The loop file (docs/tend2/rules-prod.tend2.html) is your full context — read it, including its narrative. It is READ-ONLY for you.\n\n## Checks to close\n- (code) S2 warns on each swallowed-error shape per language in prod files and stays silent when the error is logged, wrapped, rethrown, or the empty catch carries a comment naming why · evidence expected at: tests/rule_s2.rs\n- (code) S3 warns on each debug leftover shape per language in prod files and stays silent in test files, in `main.rs`, and in a module the config names as a CLI entry · evidence expected at: tests/rule_s3.rs\n- (code) D1 warns on each manifest and lockfile per language, blocks when a scope excludes the manifest, and stays silent on a README that mentions a package · evidence expected at: tests/rule_d1.rs\n- (code) D2 fires at block level on a fixture where a layer imports from a layer `[deps]` forbids, per language, and on weed's own tree when a core file imports from seams; stays silent on allowed directions · evidence expected at: tests/rule_d2.rs\n- (code) X2 fires at block level on a file outside the given scope globs, names the callers of its changed definitions, and stays silent without `--scope` and on files inside the scope · evidence expected at: tests/rule_x2.rs\n- (code) C2 warns when an ignore file gains a pattern matching source or test paths and stays silent on build-output patterns · evidence expected at: tests/rule_c2.rs\n- (code) G2 warns on an added file above 1 MiB and on an added binary, and stays silent on a small text file and on a tracked binary that only changes · evidence expected at: tests/rule_g2.rs\n\n## Definition of done — verify yourself before finishing\nYou are done when this exact command exits 0 (it is the gate the conductor will run):\n\n    tend2 verify docs/tend2/rules-prod.tend2.html --repo-root . --force --expect-payload ee57e2e792d2 --runner 'bash scripts/check/run.sh {evidence}'\n\nRun it yourself. If it does not exit 0, you are not done. Do not declare success in prose that a command disagrees with.\n\n## Already tried (do not repeat)\n- 2026-09-05 the catalogue is complete after this loop: eighteen check rules with G1 on check-face; `docs/rules.md` is written here and `weed rules` reads from the same table, so the two cannot drift\n- 2026-09-05 S2's Go shape is deliberately narrow (`return nil` after a nil check with nothing else in the block) because Go's idiom makes wider shapes noisy; calibration decides whether it stays a warning\n\n## Rules\n- Write the failing test first where it does not exist; then make it pass.\n- You must not edit the loop file — check states belong to the verifier.\n- You must not weaken, skip, or mock the unit under test to force a green; mocked-unit evidence is detected and refused.\n- Solve the CLASS of problem the claim names, not the test's example inputs. Hardcoding fixture strings, phrases, or patterns from the check or its tests into source (lookup tables, regex lists, phrase maps) is refused work even when the test goes green — it is an answer key, not a product.\n- If the claim cannot be met generically with what you have, that is a real finding: write BLOCKED.md naming the missing capability (a model, a library, a redesign, a split of the check into honest tiers) instead of faking generality.\n- Production quality only: no stubs, no TODOs, no placeholder implementations; match the style of the surrounding code.\n- Probe/scratch files (toolchain checks, experiments) go in `.loop-scratch/` only — it is never collected; scratch files elsewhere pollute the delivery.\n- If you are genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs. An honest BLOCKED.md is a good outcome; a faked green is the worst possible one."
      },
      "accept": {
        "smoke": "tend2 verify docs/tend2/rules-prod.tend2.html --repo-root . --force --expect-payload ee57e2e792d2 --runner 'bash scripts/check/run.sh {evidence}'",
        "audit": {
          "command": "tend2 verify docs/tend2/rules-prod.tend2.html --repo-root . --force --expect-payload ee57e2e792d2 --audit-egress",
          "provider": "codex",
          "selfIntegrity": true
        }
      },
      "policy": {
        "timeoutMs": 5400000
      },
      "needs": [
        "rules-tests"
      ],
      "worker": {
        "provider": "claude",
        "model": "opus"
      }
    },
    {
      "id": "rules-tests",
      "work": {
        "prompt": "You are a worker in a build loop. Make the checks below pass, honestly.\nNobody reviews your prose — only the verifier's verdict counts, so put all effort into the work itself.\n\n## The goal\nThe rules that read the change to a test suite and name each way it can become weaker without anyone deleting a test: assertions dropped, a tolerance widened, expected values regenerated, an error assertion loosened, a test uncollected by rename, and a mock placed over the very unit under change. T2 blocks by default; the rest warn, for the human at the pull request. Each lands as a failing fixture first in TypeScript/JavaScript, Python, Rust and Go, with a silent neighbour.\n\n## Your environment\n- You are at the repository root of an isolated git worktree. Work here directly.\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- The loop file (docs/tend2/rules-tests.tend2.html) is your full context — read it, including its narrative. It is READ-ONLY for you.\n\n## Checks to close\n- (code) T2 fires at block level when a changed test file's assertion count drops, per language, and stays silent when assertions move between cases with the count unchanged or a `Weed-allow: T2` trailer carries a reason · evidence expected at: tests/rule_t2.rs\n- (code) T4 warns when a paired removed/added line widens an approximate assertion or a timeout, per language, and stays silent when the number tightens or an unrelated literal changes · evidence expected at: tests/rule_t4.rs\n- (code) T5 warns when a snapshot, golden or fixture file changes alongside a prod file, and stays silent when only the snapshot changes or only prod changes · evidence expected at: tests/rule_t5.rs\n- (code) T6 warns on each weakened error assertion shape per language and stays silent when the assertion becomes more specific · evidence expected at: tests/rule_t6.rs\n- (code) T7 fires at block level when a rename takes a test file or case out of the runner's naming convention, per language, and stays silent on a rename that stays inside it · evidence expected at: tests/rule_t7.rs\n- (code) M1 warns when a test in the diff mocks a module whose prod file is also in the diff, per language, and stays silent when the mocked module is outside the diff · evidence expected at: tests/rule_m1.rs\n\n## Definition of done — verify yourself before finishing\nYou are done when this exact command exits 0 (it is the gate the conductor will run):\n\n    tend2 verify docs/tend2/rules-tests.tend2.html --repo-root . --force --expect-payload ddf4647f0225 --runner 'bash scripts/check/run.sh {evidence}'\n\nRun it yourself. If it does not exit 0, you are not done. Do not declare success in prose that a command disagrees with.\n\n## Already tried (do not repeat)\n- 2026-09-05 assertion counting is a vocabulary per language and framework; the vocabulary lives in one table in `src/core/rules/check/vocab.rs` so T2, T4 and T6 share it and `weed rules` can print it\n- 2026-09-05 T7 blocks because an uncollected test is a deleted test that still looks present; the fixture's neighbour is the same rename kept collectable\n\n## Rules\n- Write the failing test first where it does not exist; then make it pass.\n- You must not edit the loop file — check states belong to the verifier.\n- You must not weaken, skip, or mock the unit under test to force a green; mocked-unit evidence is detected and refused.\n- Solve the CLASS of problem the claim names, not the test's example inputs. Hardcoding fixture strings, phrases, or patterns from the check or its tests into source (lookup tables, regex lists, phrase maps) is refused work even when the test goes green — it is an answer key, not a product.\n- If the claim cannot be met generically with what you have, that is a real finding: write BLOCKED.md naming the missing capability (a model, a library, a redesign, a split of the check into honest tiers) instead of faking generality.\n- Production quality only: no stubs, no TODOs, no placeholder implementations; match the style of the surrounding code.\n- Probe/scratch files (toolchain checks, experiments) go in `.loop-scratch/` only — it is never collected; scratch files elsewhere pollute the delivery.\n- If you are genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs. An honest BLOCKED.md is a good outcome; a faked green is the worst possible one."
      },
      "accept": {
        "smoke": "tend2 verify docs/tend2/rules-tests.tend2.html --repo-root . --force --expect-payload ddf4647f0225 --runner 'bash scripts/check/run.sh {evidence}'",
        "audit": {
          "command": "tend2 verify docs/tend2/rules-tests.tend2.html --repo-root . --force --expect-payload ddf4647f0225 --audit-egress",
          "provider": "codex",
          "selfIntegrity": true
        }
      },
      "policy": {
        "timeoutMs": 5400000
      },
      "needs": [
        "rules-block"
      ],
      "worker": {
        "provider": "claude",
        "model": "opus"
      }
    },
    {
      "id": "sarif",
      "work": {
        "prompt": "You are a worker in a build loop. Make the checks below pass, honestly.\nNobody reviews your prose — only the verifier's verdict counts, so put all effort into the work itself.\n\n## The goal\nweed has no findings schema of its own. `sarif::render(findings, context) -> Log` produces SARIF 2.1.0 that validates against the official schema vendored at `schemas/sarif-schema-2.1.0.json`, so GitHub code scanning, every editor and every CI read weed without an adapter. A terminal gets a table instead. `docs/sarif.md` records the conventions so a consumer never guesses.\n\n## Your environment\n- You are at the repository root of an isolated git worktree. Work here directly.\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- The loop file (docs/tend2/sarif.tend2.html) is your full context — read it, including its narrative. It is READ-ONLY for you.\n\n## Checks to close\n- (code) `sarif::render` output validates against `schemas/sarif-schema-2.1.0.json` for an empty log, a single finding, many findings across levels, a finding with a fix, a suppressed finding, and a could-not-run log · evidence expected at: tests/sarif_schema.rs\n- (code) each result carries rule id and index, the level mapping (block→error, warn→warning, suppressed→note), a repo-relative uri with a region, a what/why/next message, `fixes` when mechanical, and `suppressions` with the reason when suppressed; the tool component lists every catalogue rule with its default level · evidence expected at: tests/sarif_shape.rs\n- (code) the table renders one line per finding in level-then-path-then-line order and ends with a count per level · evidence expected at: tests/sarif_table.rs\n- (code) `docs/sarif.md` names every convention above and, for each, the test function that pins it; a script checks each named test exists · evidence expected at: scripts/check/docs-sarif.sh\n\n## Definition of done — verify yourself before finishing\nYou are done when this exact command exits 0 (it is the gate the conductor will run):\n\n    tend2 verify docs/tend2/sarif.tend2.html --repo-root . --force --expect-payload 1c6929baea87 --runner 'bash scripts/check/run.sh {evidence}'\n\nRun it yourself. If it does not exit 0, you are not done. Do not declare success in prose that a command disagrees with.\n\n## Already tried (do not repeat)\n- 2026-09-05 schema validation runs in-process with the `jsonschema` crate against the vendored file, never against the network; the schema is draft-04 and the crate supports it\n- 2026-09-05 no `partialFingerprints` in v1: GitHub derives them from location when absent, and a stable fingerprint across renames is a later loop\n\n## Rules\n- Write the failing test first where it does not exist; then make it pass.\n- You must not edit the loop file — check states belong to the verifier.\n- You must not weaken, skip, or mock the unit under test to force a green; mocked-unit evidence is detected and refused.\n- Solve the CLASS of problem the claim names, not the test's example inputs. Hardcoding fixture strings, phrases, or patterns from the check or its tests into source (lookup tables, regex lists, phrase maps) is refused work even when the test goes green — it is an answer key, not a product.\n- If the claim cannot be met generically with what you have, that is a real finding: write BLOCKED.md naming the missing capability (a model, a library, a redesign, a split of the check into honest tiers) instead of faking generality.\n- Production quality only: no stubs, no TODOs, no placeholder implementations; match the style of the surrounding code.\n- Probe/scratch files (toolchain checks, experiments) go in `.loop-scratch/` only — it is never collected; scratch files elsewhere pollute the delivery.\n- If you are genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs. An honest BLOCKED.md is a good outcome; a faked green is the worst possible one."
      },
      "accept": {
        "smoke": "tend2 verify docs/tend2/sarif.tend2.html --repo-root . --force --expect-payload 1c6929baea87 --runner 'bash scripts/check/run.sh {evidence}'",
        "audit": {
          "command": "tend2 verify docs/tend2/sarif.tend2.html --repo-root . --force --expect-payload 1c6929baea87 --audit-egress",
          "provider": "codex",
          "selfIntegrity": true
        }
      },
      "policy": {
        "timeoutMs": 5400000
      },
      "needs": [
        "core"
      ],
      "worker": {
        "provider": "claude",
        "model": "opus"
      }
    },
    {
      "id": "scan",
      "work": {
        "prompt": "You are a worker in a build loop. Make the checks below pass, honestly.\nNobody reviews your prose — only the verifier's verdict counts, so put all effort into the work itself.\n\n## The goal\n`weed scan` reads the repository as it is, not a diff, and names repo-state hygiene: a path, command, flag or symbol cited in the docs that no longer exists; a public symbol nothing references; a TODO older than the configured age; a dependency pin far behind the registry. It never blocks: exit 0 or 3 only, findings at warning level. tend2's hygiene loop is its first consumer, citing `weed scan --format sarif` as evidence; `docs/tend2-seam.md` is the proposal for that seam.\n\n## Your environment\n- You are at the repository root of an isolated git worktree. Work here directly.\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- The loop file (docs/tend2/scan.tend2.html) is your full context — read it, including its narrative. It is READ-ONLY for you.\n\n## Checks to close\n- (code) `weed scan` reads the tree rather than the diff (an unstaged and an uncommitted change make no difference), exits 0 with warnings and 3 when it cannot run, and prints SARIF or a table like `check` · evidence expected at: tests/scan_face.rs\n- (code) R1 warns on a cited path, command, flag and symbol that no longer resolve, one fixture each, and stays silent when they all resolve · evidence expected at: tests/rule_r1.rs\n- (code) R2 warns on an exported symbol nothing references in each of ts, py, rs and go and stays silent on a referenced one and on the exempt entry points · evidence expected at: tests/rule_r2.rs\n- (code) R3 warns on a TODO whose blame date is older than the threshold and stays silent on a fresh one · evidence expected at: tests/rule_r3.rs\n- (code) R4 warns on a pin behind the committed snapshot beyond the threshold for each manifest kind and stays silent within it, with no network reached · evidence expected at: tests/rule_r4.rs\n- (code) `docs/tend2-seam.md` shows a tend2 check citing `weed scan --format sarif` as evidence and proposes how tend2's verifier could call `weed check` in place of its `mock.ts`; the commands it shows run and exit as it says · evidence expected at: scripts/check/docs-tend2-seam.sh\n\n## Definition of done — verify yourself before finishing\nYou are done when this exact command exits 0 (it is the gate the conductor will run):\n\n    tend2 verify docs/tend2/scan.tend2.html --repo-root . --force --expect-payload c7a030177d12 --runner 'bash scripts/check/run.sh {evidence}'\n\nRun it yourself. If it does not exit 0, you are not done. Do not declare success in prose that a command disagrees with.\n\n## Already tried (do not repeat)\n- 2026-09-05 settled with tend2 on the channel (umbrella loop, 2026-09-05): `scan` owns repo-state hygiene and never blocks; tend2 keeps doc-evidence staleness and its hygiene loop pattern; `tend2 gate` and `weed check` are different questions and both emit SARIF\n- 2026-09-05 R4 never reaches the network during `scan`; the snapshot is a committed file so the rule is reproducible in CI\n\n## Rules\n- Write the failing test first where it does not exist; then make it pass.\n- You must not edit the loop file — check states belong to the verifier.\n- You must not weaken, skip, or mock the unit under test to force a green; mocked-unit evidence is detected and refused.\n- Solve the CLASS of problem the claim names, not the test's example inputs. Hardcoding fixture strings, phrases, or patterns from the check or its tests into source (lookup tables, regex lists, phrase maps) is refused work even when the test goes green — it is an answer key, not a product.\n- If the claim cannot be met generically with what you have, that is a real finding: write BLOCKED.md naming the missing capability (a model, a library, a redesign, a split of the check into honest tiers) instead of faking generality.\n- Production quality only: no stubs, no TODOs, no placeholder implementations; match the style of the surrounding code.\n- Probe/scratch files (toolchain checks, experiments) go in `.loop-scratch/` only — it is never collected; scratch files elsewhere pollute the delivery.\n- If you are genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs. An honest BLOCKED.md is a good outcome; a faked green is the worst possible one."
      },
      "accept": {
        "smoke": "tend2 verify docs/tend2/scan.tend2.html --repo-root . --force --expect-payload c7a030177d12 --runner 'bash scripts/check/run.sh {evidence}'",
        "audit": {
          "command": "tend2 verify docs/tend2/scan.tend2.html --repo-root . --force --expect-payload c7a030177d12 --audit-egress",
          "provider": "codex",
          "selfIntegrity": true
        }
      },
      "policy": {
        "timeoutMs": 5400000
      },
      "needs": [
        "check-face",
        "tilth-core"
      ],
      "worker": {
        "provider": "claude",
        "model": "opus"
      }
    },
    {
      "id": "tilth-core",
      "work": {
        "prompt": "You are a worker in a build loop. Make the checks below pass, honestly.\nNobody reviews your prose — only the verifier's verdict counts, so put all effort into the work itself.\n\n## The goal\nweed reads code through `tilth-core`, a library crate extracted from tilth that owns tree-sitter outlines, file-type and language detection, test-file classification, import extraction and resolution, and caller search across fourteen languages. weed does not parse anything itself. All of that reading sits behind one seam module, `src/seams/reader.rs`, so the core stays pure and the dependency can be swapped or upgraded mechanically.\n\n## Your environment\n- You are at the repository root of an isolated git worktree. Work here directly.\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- The loop file (docs/tend2/tilth-core.tend2.html) is your full context — read it, including its narrative. It is READ-ONLY for you.\n\n## Checks to close\n- (code) the tilth worktree at `.context/tilth-core` is on branch `tilth-core`, holds `crates/tilth-core` exposing detection, outline entries, test-file classification, test structure, imports and callers, tilth's binary consumes it, and tilth's CI trio is green there · evidence expected at: scripts/check/tilth-core.sh\n- (code) weed's `Cargo.toml` depends on `tilth-core` as a git dependency on the tilth repository, `.cargo/config.toml` patches it to the local worktree, and `cargo build` succeeds · evidence expected at: scripts/check/tilth-core-dep.sh\n- (code) `src/seams/reader.rs` is the only file in `src/` that references `tilth_core`, and `src/core/` compiles with the reader's data types only · evidence expected at: tests/reader_seam.rs\n- (code) the reader returns outline entries, test shape, imports and callers for one small file in each of ts, py, rs and go, asserted against the fixture's known contents · evidence expected at: tests/reader_langs.rs\n\n## Definition of done — verify yourself before finishing\nYou are done when this exact command exits 0 (it is the gate the conductor will run):\n\n    tend2 verify docs/tend2/tilth-core.tend2.html --repo-root . --force --expect-payload 524a785fdf0b --runner 'bash scripts/check/run.sh {evidence}'\n\nRun it yourself. If it does not exit 0, you are not done. Do not declare success in prose that a command disagrees with.\n\n## Already tried (do not repeat)\n- 2026-09-05 the brief named the modules on an older branch of tilth (`src/read/outline/`, `src/search/symbol.rs`, `src/search/deps.rs`, `src/types.rs::is_test_file`, `src/search/blast.rs`); on main the shared language infrastructure has since moved to `src/lang/`, so the extraction follows main's layout, not the brief's file list\n- 2026-09-05 tilth's `main` trips one clippy lint on rustc 1.98 (`manual_is_variant_and` in `src/overview.rs:693`) that its CI toolchain did not know; the extraction plan fixes that one line so the CI trio can be the gate\n- 2026-09-05 decided: weed does not fork or vendor any tilth parsing code; if the crate is not green, weed waits, and the reader seam is the only place the wait is felt\n\n## Rules\n- Write the failing test first where it does not exist; then make it pass.\n- You must not edit the loop file — check states belong to the verifier.\n- You must not weaken, skip, or mock the unit under test to force a green; mocked-unit evidence is detected and refused.\n- Solve the CLASS of problem the claim names, not the test's example inputs. Hardcoding fixture strings, phrases, or patterns from the check or its tests into source (lookup tables, regex lists, phrase maps) is refused work even when the test goes green — it is an answer key, not a product.\n- If the claim cannot be met generically with what you have, that is a real finding: write BLOCKED.md naming the missing capability (a model, a library, a redesign, a split of the check into honest tiers) instead of faking generality.\n- Production quality only: no stubs, no TODOs, no placeholder implementations; match the style of the surrounding code.\n- Probe/scratch files (toolchain checks, experiments) go in `.loop-scratch/` only — it is never collected; scratch files elsewhere pollute the delivery.\n- If you are genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs. An honest BLOCKED.md is a good outcome; a faked green is the worst possible one."
      },
      "accept": {
        "smoke": "tend2 verify docs/tend2/tilth-core.tend2.html --repo-root . --force --expect-payload 524a785fdf0b --runner 'bash scripts/check/run.sh {evidence}'",
        "audit": {
          "command": "tend2 verify docs/tend2/tilth-core.tend2.html --repo-root . --force --expect-payload 524a785fdf0b --audit-egress",
          "provider": "codex",
          "selfIntegrity": true
        }
      },
      "policy": {
        "timeoutMs": 5400000
      },
      "needs": [
        "core"
      ],
      "worker": {
        "provider": "claude",
        "model": "opus"
      }
    },
    {
      "id": "weed",
      "work": {
        "prompt": "You are a worker in a build loop. Make the checks below pass, honestly.\nNobody reviews your prose — only the verifier's verdict counts, so put all effort into the work itself.\n\n## The goal\nA static Rust binary that reads what an agent produced and refuses the growth that should not be there — deleted or weakened tests, skips, stubs, swallowed errors, secrets, guardrail edits, dependency-direction violations — in milliseconds, with zero tokens, every finding as SARIF 2.1.0. `weed check` judges a diff and may block; `weed scan` judges the tree and never blocks; `weed guard` is the law in git through hooks; `weed bite` proves a test fails without its change. For maintainers drowning in agent-authored pull requests who need a deterministic gate before they read, for teams running agents on several harnesses who want one law, and for the garden's own fleets through pleach.\n\n## Your environment\n- You are at the repository root of an isolated git worktree. Work here directly.\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- The loop file (docs/tend2/weed.tend2.html) is your full context — read it, including its narrative. It is READ-ONLY for you.\n\n## Checks to close\n- (code) every rule the catalogue prints (`weed rules`) has a fire and a silent fixture for each of ts, py, rs and go under `fixtures/adversarial/`, and one test walks that matrix, replays each fixture through the real binary and asserts the expected result set · evidence expected at: tests/catalogue_matrix.rs\n- (code) `weed check --base <root-commit> --format sarif` on weed's own tree reports nothing at block level, and the log validates against the vendored SARIF schema · evidence expected at: tests/self_check.rs\n- (code) `weed check` on a fixture repo finishes in under 200 milliseconds wall-clock for a 50-file diff, measured by the test on the release binary · evidence expected at: tests/speed.rs\n\n## Definition of done — verify yourself before finishing\nYou are done when this exact command exits 0 (it is the gate the conductor will run):\n\n    tend2 verify docs/tend2/weed.tend2.html --repo-root . --force --expect-payload 5adb335edcb1 --runner 'bash scripts/check/run.sh {evidence}'\n\nRun it yourself. If it does not exit 0, you are not done. Do not declare success in prose that a command disagrees with.\n\n## Already tried (do not repeat)\n- 2026-09-05 map shaped from the umbrella's fit loop and the build brief (revised 2026-09-05), before any code, as the garden law requires; the umbrella map holds fit (F1 to F6), this map holds modules\n- 2026-09-05 weed lives in its own repository, not the tilth Cargo workspace: cape-town's correction over the channel and the brief's revision agree; `building-the-garden.md` §5 and §8 still say \"in the tilth workspace\" and are reported stale to the umbrella rather than followed\n- 2026-09-05 process requirement from the owner: build with tend2 loops, pleach conducting, umbel driving, and record every experience with those tools in `docs/dogfood.md`. The brief's \"no subagents\" line yields to that: workers are pleach nodes, this map is their work order, and the conductor audits the result against the diff\n- 2026-09-05 `tilth-core` does not exist; the extraction is conducted first as its own pleach plan against a worktree of tilth (`plans/tilth-core.plan.json`), then this map's plan is emitted. Until the branch is pushed, `.cargo/config.toml` patches the git dependency to the local worktree — the one machine-specific line in the repo, flagged above\n- 2026-09-05 `--strict` defined, since the brief uses it without saying what it does: suppressions are reported but not honoured, and a rule that cannot evaluate a file makes the run fail closed (exit 3) instead of skipping the file. A gate an agent can talk out of a block with an allowance it wrote itself is not a gate\n- 2026-09-05 fixtures are directory pairs (`before/` committed, `after/` as the working tree), not patch files, so a fixture reads as code in its language and the test replays it through the real git path\n- 2026-09-05 the check rules are split across three sequential loops (block, tests, prod) rather than parallel siblings: they share the rule registry file and parallel edits there would merge-conflict on every join; guard, hooks and scan run in parallel with rules-tests since they own disjoint files\n- 2026-09-05 calibration repos follow the fit loop (tilth, pleach, tend2, copeca, umbel) rather than the brief's list with rctrl; several have fewer than 200 commits on their default branch, so the run takes every commit available and reports the count\n- 2026-09-05 scope-out: no rules that name vendors or products; no inbound injection quarantine; no spend rules (settled on the umbrella loop, not re-argued here)\n\n## Rules\n- Write the failing test first where it does not exist; then make it pass.\n- You must not edit the loop file — check states belong to the verifier.\n- You must not weaken, skip, or mock the unit under test to force a green; mocked-unit evidence is detected and refused.\n- Solve the CLASS of problem the claim names, not the test's example inputs. Hardcoding fixture strings, phrases, or patterns from the check or its tests into source (lookup tables, regex lists, phrase maps) is refused work even when the test goes green — it is an answer key, not a product.\n- If the claim cannot be met generically with what you have, that is a real finding: write BLOCKED.md naming the missing capability (a model, a library, a redesign, a split of the check into honest tiers) instead of faking generality.\n- Production quality only: no stubs, no TODOs, no placeholder implementations; match the style of the surrounding code.\n- Probe/scratch files (toolchain checks, experiments) go in `.loop-scratch/` only — it is never collected; scratch files elsewhere pollute the delivery.\n- If you are genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs. An honest BLOCKED.md is a good outcome; a faked green is the worst possible one."
      },
      "accept": {
        "smoke": "tend2 verify docs/tend2/weed.tend2.html --repo-root . --force --expect-payload 5adb335edcb1 --runner 'bash scripts/check/run.sh {evidence}'",
        "audit": {
          "command": "tend2 verify docs/tend2/weed.tend2.html --repo-root . --force --expect-payload 5adb335edcb1 --audit-egress",
          "provider": "codex",
          "selfIntegrity": true
        }
      },
      "policy": {
        "timeoutMs": 5400000
      },
      "needs": [
        "tilth-core",
        "core",
        "sarif",
        "check-face",
        "rules-block",
        "rules-tests",
        "rules-prod",
        "guard",
        "hooks",
        "scan",
        "calibration",
        "bite",
        "garden-fit"
      ],
      "worker": {
        "provider": "claude",
        "model": "opus"
      }
    }
  ],
  "warnings": [
    {
      "loopId": "core",
      "code": "unwired-modules",
      "message": "5 checks cite 2 src modules (AGENTS.md, CLAUDE.md), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "rules-block",
      "code": "unwired-modules",
      "message": "6 checks cite 2 src modules (AGENTS.md, CLAUDE.md), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "scan",
      "code": "unwired-modules",
      "message": "6 checks cite 3 src modules (CLAUDE.md, AGENTS.md, /), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "tilth-core",
      "code": "unwired-modules",
      "message": "4 checks cite 2 src modules (.context/tilth-core, plans/tilth-core.plan.json), but no single check's evidence spans 2+ of them"
    }
  ]
}
