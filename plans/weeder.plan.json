{
  "goal": "A static Rust binary that reads what an agent produced and refuses the growth that should not be there, deleted or weakened tests, skips, stubs, swallowed errors, secrets, guardrail edits, dependency-direction violations, in milliseconds, with zero tokens, every finding as SARIF 2.1.0. `weeder check` judges a diff and may block; `weeder scan` judges the tree and never blocks; `weeder guard` is the law in git through hooks; `weeder bite` proves a test fails without its change. For maintainers drowning in agent-authored pull requests who need a deterministic gate before they read, for teams running agents on several harnesses who want one law, and for the garden's own fleets through pleach.",
  "source": "docs/tend2",
  "nodes": [
    {
      "id": "calibration-audit",
      "work": {
        "prompt": "You are a worker in a build loop. Make the checks below pass, honestly.\nNobody reviews your prose — only the verifier's verdict counts, so put all effort into the work itself.\n\n## The goal\nThe classification of blocked commits into true positive, acceptable and false positive is the number weeder ships on, so it is not the builder's word. A second party on a different provider re-classifies a random sample of at least twenty blocked commits from `docs/calibration-2026-09.md` and at least twenty cases from the recall sample, blind to the first classification, and writes `docs/calibration-audit-2026-09.md`. Agreement at or above 90 percent on each sample stands; below it, the classification is untrusted and redone before anything ships.\n\n## Your environment\n- You are at the repository root of an isolated git worktree. Work here directly.\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- The loop file (docs/tend2/calibration-audit.tend2.html) is your full context — read it, including its narrative. It is READ-ONLY for you.\n\n## Checks to close\n- (code) the seal covers the judged content alone: a case packet carries the rule ids, each finding's level, path and line, and the commit's hunks, and no finding sentence, so the wording a message teaches can change without a re-seal; `cargo xtask audit-packet` writes that shape, the fairness and transcript evidence hold it, and the packets are regenerated under the renamed token with forty fresh blind reads and the sighted run recorded on a fresh seed by `scripts/audit/blind-run.sh` and `scripts/audit/sighted-run.sh`, every session's answer opening with its packet's hash and the agreement recomputed · evidence expected at: scripts/check/audit-fairness.sh\n\n## Definition of done — verify yourself before finishing\nYou are done when this exact command exits 0 (it is the gate the conductor will run):\n\n    tend2 verify docs/tend2/calibration-audit.tend2.html --repo-root . --force --expect-payload c04b654625a9 --runner 'bash scripts/check/run.sh {evidence}'\n\nRun it yourself. If it does not exit 0, you are not done. Do not declare success in prose that a command disagrees with.\n\n## Already tried (do not repeat)\n- 2026-09-05 shaped from cape-town's binding checks; the owner may take the auditor's seat instead of a second provider, and the script does not care which\n- 2026-09-06 conductor: the pleach running these nodes stages every path the Write and Edit tools touched, wherever it is. A path outside this worktree (under `$TMPDIR` or `/tmp`) makes the collector ask git about a file it cannot see, which kills the delivery and removes the worktree (dogfood P8, P11); `.loop-scratch/` is collected and judged by the hygiene gate (P10). So: never use the Write or Edit tool outside this worktree; write scratch outside it only through the shell; leave `.loop-scratch/` empty before you finish; and never leave a credential-shaped string, even a quoted one, in a file that will be collected\n- 2026-09-06 built by pleach node `calibration-audit` (codex worker, claude audit), both checks stamped, landed through `pleach land`. `docs/calibration-audit-2026-09.md` re-grades twenty blocked commits and twenty recall cases drawn by SHA-256 over a seed, and agrees with the builder's classification on every one of both samples; `scripts/check/calibration-agreement.sh` redraws the sample and recomputes the agreement. Total agreement is a strong result and a reason to look at the draw: the sample comes from the calibration file's own tables, where the builder's class sits beside the commit, so the auditor could read it before judging. A later audit should hand the auditor the diffs and nothing else\n- 2026-09-06 cape-town, on the anchoring caveat: the unqualified verdict stands today on the sighted audit, and a blind one is what makes it stand tomorrow. Shaped above: the auditor receives diffs and rule ids only, never the class, on a fresh seed, same 90 percent bar, and every audit file says which kind it is so the report can read it\n- 2026-09-06 the first blind run (codex, node/calibration-audit) came back forty of forty and is refused unlanded: its packet's `diff` blocks were not diffs but sentences the same worker wrote after reading the ledger (`+ The command surface consolidated file listing into the list tool.`), which is the builder's reasoning handed to the auditor in a code fence. The check is reworded so blindness is a property a script can refuse: verbatim git hunks from the pinned corpus, a fresh session, and the packet re-derived at verify time\n- 2026-09-06 cape-town, after the refusal: independence of information is derived by code and re-derived at verify time, never declared by an agent. Three checks above: the packet is code's alone and regenerated at verify; the auditor's transcript shows nothing but the packet; and the refused audit stays in the tree as a specimen the agreement script never reads. The specimen is in place already, so the next builder sees what a false blind looks like\n- 2026-09-06 the mechanical blind run (codex, seed `calibration-audit-blind-2026-09-47`, packet sha256 d6791b87a99c9d35…, verbatim hunks from the pinned corpus) agreed on 9 of 20 blocked commits and 12 of 20 recall cases, under the bar, so the classification is untrusted and the verdict is pending again. Read case by case, the disagreement is mostly the packet's poverty rather than the ledger's: with rule ids and hunks alone the auditor judged whether a change was bad, not whether the rule's claim was true of it (it called an X1 hit on css class names a true positive as an injection risk, and every deleted test file a true positive without the replacement the builder had read), and on recall it found planted cases with no visible shape (a D1 case that only changed a version string, a T6 case that broke a test signature), which is a finding about the injector worth its own look. Shaped above as a fairness check: the packet carries what weeder itself printed, never what the builder wrote\n- 2026-09-06 cape-town accepted the fairness redesign with one requirement, written into the check above: the packet carries the commit's full diff, every hunk, plus weeder's printed finding, so the auditor can see replacement coverage and judge whether the rule's claim is true of the change rather than whether the change is bad. The poverty diagnosis is a hypothesis and the fair packet is its test: still under the bar with a fair packet means the ledger is wrong and is redone\n- 2026-09-06 the fairness node (codex) regenerated a fair packet, with catalogue text, weeder's findings, the planted site and the question, and kept the old response under it: the agreement table is still seed 47's 9 of 20 and 12 of 20, judged on the poor packet, and the transcript is a nineteen-line note saying a session happened. Two stamps refused. The evidence now demands the linkage a note cannot fake: the response declares the sha256 of the packet it answered and it has to be the fair packet's, and the transcript is the session's own record with the prompt and the verbatim answer. Seed 47 is spent; the fair run takes a fresh one\n- 2026-09-06 the fair run (codex, two attempts) found the real limit: a packet of twenty full diffs and twenty recall cases is 60,000 lines, and the fresh session answered `Codex ran out of room in the model's context window`; a second attempt capped each section at 75 kB, got an answer in 92,000 input tokens, and was then refused at pleach's hygiene gate because a scratch copy of the packet under `.loop-scratch/` quoted the AWS documentation key from a sampled commit (dogfood, 2026-09-06). Quarantined at quarantine/calibration-audit. Reshaped above: one case, one file, one fresh session, one answer naming its file's hash; a diff past the generator's stated cap is reported as capped rather than cut. `scripts/trim-plan.sh` now tells workers where scratch goes, since the emitted prompt still says `.loop-scratch/` is never collected\n- 2026-09-06 the per-case node (codex) closed with every check stamped and is refused a third time: its forty `sessions/*.events.jsonl` are two synthetic lines each, no codex thread or turn, the seed is still 47, and 31 of the 40 answers are the old response's reasoning verbatim. The worker wrote the evidence scripts and the artefacts they judge, and both agreed. The conductor now runs the sessions: `scripts/audit/blind-run.sh <seed>` regenerates the case packets, hands each to a fresh `codex exec` in an empty directory with config and rules ignored, keeps the raw event stream as the record, and assembles the audit file from the answers; `audit-transcript.sh` refuses a record without a real thread, turn and one agent message. The generator, the case layout and the specimen from the node are kept\n- 2026-09-06 the conductor's runner produced the first real blind re-grade: seed `calibration-audit-blind-2026-09-fair-1`, forty fresh `codex exec` sessions, no answer copied from any earlier run. Blocked commits 9 of 20 (six of the eleven disagreements are the auditor calling `false-positive` where the ledger says `acceptable` on T1 deletions whose cases moved, which is the finding's own sentence, `covered by nobody`, being untrue of the change; three are T2/S1 swaps between true-positive and acceptable; one is a capped diff the auditor could not see). Recall 12 of 20, with eight `not-a-case` answers, six of them D1 version-string plants and two T6 signature breaks, the injector defects recall now owns. Then the recall node, running at the same time, rewrote the campaign cache under `$TMPDIR` that the packet generator reads recall cases from, and thirteen recall packets stopped regenerating: the generator reads a cache, so the packet is only reproducible while nothing else touches it. Shaped above as re-planting from the pinned commit. The blocked half of this re-grade stands; the recall half is re-drawn once the injector lands\n- 2026-09-06 the fair packets put the corpus's own hazards in weeder's tree: one spelled the AWS documentation key whole and carried a private-key fixture, so `tests/rule_x1.rs` and `tests/self_check.rs` went red on the packets' arrival. Two answers, both landed: the generator writes a published example as a named marker (`<published example credential, stamp AKIA>`), and the packets and session records live under `fixtures/adversarial/calibration-audit/`, the specimen root cape-town's ruling allows, where weeder reports them as notes. The session records still name the old path as their input, which the transcript evidence refuses until the next fair run writes new ones\n- 2026-09-06 the calibration worker re-ran the blind sessions with `scripts/audit/blind-run.sh` on seed `calibration-audit-blind-2026-09-redo-2`, forty real `codex exec` records: blocked commits 14 of 20 against the redone ledger, recall cases 20 of 20 with the fixed injector. Sighted on the same draw, 40 of 40, which is the anchoring caveat measured rather than argued. Blocked agreement is under the bar, so the verdict stays qualified and the first sentence says the classification is untrusted; the six remaining disagreements are the acceptable-versus-true-positive line, where the auditor and the builder read the same true claim and disagree on whether the change weakened something\n- 2026-09-06 the cache-independence check had been stamped by `audit-fairness.sh`, which never tests it. `scripts/check/packet-cache-free.sh` now derives the seed's packets twice, once from an empty `WEEDER_RECALL_CACHE` and once from this machine's, and five T6 recall packets differ: the replay of a recall case still reads something the campaign left behind. Open again, with the evidence that fails\n- 2026-09-06 cape-town's ruling on the disputed line: a blind reader can judge whether the rule's claim is true of the change and nothing else, so the audited agreement on blocked commits is binary, claim-true or claim-false, and `acceptable` is a human label the audit never counts. Recorded in both audit files with the date before recomputing. On the responses already kept: seed fair-1 on the old ledger, 14 of 20 binary against 9 of 20 old-class; seed redo-2 on the redone ledger, 17 of 20 binary against 12 of 20 old-class, both runs. The three binary disagreements were the ledger calling claim-false what the ruling's own definition calls claim-true (assertions that really dropped from a file and moved elsewhere in the diff), so those three ledger calls are redone under the ruling and the message's consequence clause goes to rules-tests as T2's overclaim\n- 2026-09-06 under the binary the kept responses agree: seed redo-2, blocked 20 of 20 (12 of 20 under the old classes, printed beside), recall 19 of 20; the sighted re-grade re-taken by `scripts/audit/sighted-run.sh` on the same draw, 20 of 20 both ways. The bar is read on the blind file alone\n- 2026-09-06 after the T2 fix moved the block set from 23 to 20, both re-grades were re-taken by script on seed `calibration-audit-blind-2026-09-t2-1`: blind 19 of 20 blocked commits and 20 of 20 recall cases, sighted 20 of 20 both ways. The verdict stands unqualified on the blind file\n- 2026-09-06 correcting the recall section moved the seeded sample by exactly one case: `recall-t6-ts-tend2-56072cdab` out, `recall-t2-py-copeca-70d669542` in, with the other 39 packets byte-identical and their seals intact. The blind half was repaired with one fresh auditor session on the new packet, which came back `miss` on its own. The sighted half needed more: its answers open with the SHA-256 of the report they were handed, and no check ever read that back, so every one of them had gone on naming a revision of `docs/calibration-2026-09.md` that no longer existed. The whole sighted half was re-run against the corrected report, and `audit-transcript.sh` now reads the anchor back and refuses a re-grade of a document that has changed under it. A hash nobody compares is decoration\n- 2026-09-08 cape-town: the tokens rename with the tool, and the seal moves off the wording so the price is paid once. The packet the SHA covers becomes the judged content, rule id, level, path, line and the hunks; the sentence that teaches the token sits outside it, since the auditor's question is whether the rule's claim is true of the hunk. Forty fresh reads follow the rename; the calibration numbers do not move\n\n## Rules\n- Write the failing test first where it does not exist; then make it pass.\n- You must not edit the loop file — check states belong to the verifier.\n- You must not weaken, skip, or mock the unit under test to force a green; mocked-unit evidence is detected and refused.\n- Solve the CLASS of problem the claim names, not the test's example inputs. Hardcoding fixture strings, phrases, or patterns from the check or its tests into source (lookup tables, regex lists, phrase maps) is refused work even when the test goes green — it is an answer key, not a product.\n- If the claim cannot be met generically with what you have, that is a real finding: write BLOCKED.md naming the missing capability (a model, a library, a redesign, a split of the check into honest tiers) instead of faking generality.\n- Production quality only: no stubs, no TODOs, no placeholder implementations; match the style of the surrounding code.\n- Probe/scratch files (toolchain checks, experiments) go in `.loop-scratch/` only — it is never collected; scratch files elsewhere pollute the delivery.\n- If you are genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs. An honest BLOCKED.md is a good outcome; a faked green is the worst possible one."
      },
      "accept": {
        "smoke": "tend2 verify docs/tend2/calibration-audit.tend2.html --repo-root . --force --expect-payload c04b654625a9 --runner 'bash scripts/check/run.sh {evidence}'",
        "audit": {
          "command": "tend2 verify docs/tend2/calibration-audit.tend2.html --repo-root . --force --expect-payload c04b654625a9 --audit-egress --runner 'bash scripts/check/run.sh {evidence}'",
          "provider": "opencode",
          "model": "deepseek/deepseek-v4-pro",
          "selfIntegrity": true
        }
      },
      "policy": {
        "timeoutMs": 5400000
      },
      "worker": {
        "provider": "codex"
      }
    },
    {
      "id": "check-face",
      "work": {
        "prompt": "You are a worker in a build loop. Make the checks below pass, honestly.\nNobody reviews your prose — only the verifier's verdict counts, so put all effort into the work itself.\n\n## The goal\n`weeder check` is the primary face: it judges the index plus the working tree against HEAD by default (what pleach's smoke gate sees after it stages a worker's files), `--staged` judges the index alone (the pre-commit face), `--base <ref>` judges the tree against a ref (CI). It prints SARIF when stdout is not a terminal and a table when it is, `--format` overrides, and it exits 0, 2 or 3 as the contract says. `weeder rules` prints the catalogue. The git seam lives here, and the first rule runs end to end: G1, conflict markers.\n\n## Your environment\n- You are at the repository root of an isolated git worktree. Work here directly.\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- The loop file (docs/tend2/check-face.tend2.html) is your full context — read it, including its narrative. It is READ-ONLY for you.\n\n## Checks to close\n- (code) the suppression tokens are `Weeder-allow:` in a commit message and `weeder-allow <RULE>:` on a line, and the gate honours only those: a `Weed-allow:` trailer neither suppresses nor draws a complaint; every finding's next sentence, `SKILL.md`, `docs/rules.md`, the README and every fixture that carries a trailer teach the new token; the reason written beside the parser in `src/core/suppress.rs` is rewritten to say what the re-seal cost and that the seal no longer covers wording · evidence expected at: tests/suppress_token.rs\n- (code) `cargo xtask suppressions` counts both spellings of the trailer in history, since a history read is not the gate, and reports them as one rate · evidence expected at: xtask/tests/suppressions.rs\n- (code) with no flags `weeder check` includes every untracked, not-ignored file as an added file (before side absent, after side read from the working tree, listed as `tree_files` lists them so the log stays deterministic), and `weeder hook <harness>` at the stop event does the same, while `--staged`, `--base` and the pre-push range keep their meaning; `--untracked <include|exclude>` defaults to include in those two modes and to exclude elsewhere, `SKILL.md` names it and `tests/skill_md.rs` holds it to `--help`; a repository whose only change is an untracked test file carrying a skip marker reports T3 plainly, nothing with `--staged`, nothing with `--untracked exclude`, byte-identical across two runs; G2 weighs an untracked blob under the mebibyte cap and X2 reports an untracked file outside the scope · evidence expected at: tests/check_untracked.rs\n\n## Definition of done — verify yourself before finishing\nYou are done when this exact command exits 0 (it is the gate the conductor will run):\n\n    tend2 verify docs/tend2/check-face.tend2.html --repo-root . --force --expect-payload 8e22d0c43cc7 --runner 'bash scripts/check/run.sh {evidence}'\n\nRun it yourself. If it does not exit 0, you are not done. Do not declare success in prose that a command disagrees with.\n\n## Already tried (do not repeat)\n- 2026-09-05 default mode chosen from pleach's own gate: `accept.smoke` runs in the node worktree after scoped staging, exit-code gated, no arguments passed (pleach 0.0.1's `src/loop/run-node.ts`, the smoke block)\n- 2026-09-05 G1 lands here, not in rules-block, so the pipeline is proven end to end by the simplest rule before the catalogue is built on it\n- 2026-09-05 the CLI is `clap` with derive; subcommands for scan, guard, bite and hook are added by their own loops, each in its own file under `src/faces/`\n- 2026-09-05 the fixture harness moved here from core's Tried: core delivered no `tests/common/mod.rs`, and this is the first loop that needs one\n- 2026-09-05 built by pleach node `check-face` (claude/opus, codex audit) in two attempts, 27 minutes; the first attempt was refused by pleach's own marker gate because the G1 fixtures carried real conflict markers, so the worker invented `{{weeder:ours}}` placeholders the harness expands on the way into the temp repo. Landed by `pleach land` as a merge commit 6537357\n- 2026-09-05 conductor review, fixed inline and re-verified: `--scope` had become a filter that dropped out-of-scope files from the judgement, which would have made X2 impossible; my own loop wording (\"restricts the judged paths\") caused it. Every file is judged now and the scope is carried for X2. And trailers were read from git's `COMMIT_EDITMSG`, which at pre-commit time holds the previous commit's message and lingers after it, so a `Weed-allow` could silence an unrelated later change; the pending message now arrives only through `--message-file`, and a test pins that the old message never counts\n- 2026-09-05 refined with the umbrella's reading of `--strict`: a suppressed finding keeps its `suppressions` entry in the SARIF and gets its rule's level back, reported, not honoured, instead of being dropped from the log; the renderer no longer forces `note` on a suppressed finding, the face decides\n- 2026-09-05 the second half of `--strict` (a file a rule cannot evaluate fails closed) has nothing to act on until the reader seam lands; the loop that lands it owns that clause\n- 2026-09-08 cape-town, deciding the trailer for the tool's life rather than for today's evidence: the tokens rename with the tool before any release, `Weeder-allow:` and `weeder-allow <RULE>:`; the price is forty fresh auditor reads, cheapest now before an external commit carries the old token, and the seal moves off the wording so it is paid once (calibration-audit's Tried). Both spellings are counted in history because a history read is not the gate; honouring both in the gate would be the shim weeder does not ship\n- 2026-09-08 owner's issue 11, from an audit of a 151-file agent tree: 131 of the changed files were untracked, plain `check` saw 3 errors and the staged tree 20, because `git diff HEAD` never lists a file that was never added; the stop event is the moment weeder exists for. Shaped above; the pre-commit hook keeps judging the index alone, because a commit carries the index\n\n## Rules\n- Write the failing test first where it does not exist; then make it pass.\n- You must not edit the loop file — check states belong to the verifier.\n- You must not weaken, skip, or mock the unit under test to force a green; mocked-unit evidence is detected and refused.\n- Solve the CLASS of problem the claim names, not the test's example inputs. Hardcoding fixture strings, phrases, or patterns from the check or its tests into source (lookup tables, regex lists, phrase maps) is refused work even when the test goes green — it is an answer key, not a product.\n- If the claim cannot be met generically with what you have, that is a real finding: write BLOCKED.md naming the missing capability (a model, a library, a redesign, a split of the check into honest tiers) instead of faking generality.\n- Production quality only: no stubs, no TODOs, no placeholder implementations; match the style of the surrounding code.\n- Probe/scratch files (toolchain checks, experiments) go in `.loop-scratch/` only — it is never collected; scratch files elsewhere pollute the delivery.\n- If you are genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs. An honest BLOCKED.md is a good outcome; a faked green is the worst possible one."
      },
      "accept": {
        "smoke": "tend2 verify docs/tend2/check-face.tend2.html --repo-root . --force --expect-payload 8e22d0c43cc7 --runner 'bash scripts/check/run.sh {evidence}'",
        "audit": {
          "command": "tend2 verify docs/tend2/check-face.tend2.html --repo-root . --force --expect-payload 8e22d0c43cc7 --audit-egress --runner 'bash scripts/check/run.sh {evidence}'",
          "provider": "opencode",
          "model": "deepseek/deepseek-v4-pro",
          "selfIntegrity": true
        }
      },
      "policy": {
        "timeoutMs": 5400000
      },
      "worker": {
        "provider": "claude",
        "model": "opus"
      }
    },
    {
      "id": "hooks",
      "work": {
        "prompt": "You are a worker in a build loop. Make the checks below pass, honestly.\nNobody reviews your prose — only the verifier's verdict counts, so put all effort into the work itself.\n\n## The goal\n`weeder hook <claude|gemini|codex>` reads a harness hook event as JSON on stdin and answers in the harness's own shape. For a tool call that runs `git commit`, it judges the index and denies with the findings as the reason; for the end of a turn (Stop on Claude Code, AfterAgent on Gemini CLI), it judges the working tree and blocks the stop with the findings, so an agent cannot declare done over a dishonest tree. Codex gets whatever its hook events can deny, and the rest is written down. Every hook is proven with a real harness run recorded in `docs/proof-2026-09.md`.\n\n## Your environment\n- You are at the repository root of an isolated git worktree. Work here directly.\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- The loop file (docs/tend2/hooks.tend2.html) is your full context — read it, including its narrative. It is READ-ONLY for you.\n\n## Checks to close\n- (code) at the stop event over a tree whose only change is an untracked production file carrying a stub, `weeder hook claude` blocks with the table as the reason, and the pre-commit hook `guard` installs still judges the index alone · evidence expected at: tests/hook_untracked.rs\n\n## Definition of done — verify yourself before finishing\nYou are done when this exact command exits 0 (it is the gate the conductor will run):\n\n    tend2 verify docs/tend2/hooks.tend2.html --repo-root . --force --expect-payload ccc3a22fbfb1 --runner 'bash scripts/check/run.sh {evidence}'\n\nRun it yourself. If it does not exit 0, you are not done. Do not declare success in prose that a command disagrees with.\n\n## Already tried (do not repeat)\n- 2026-09-05 the harness hook is the second line, not the first: `guard` is the law in git; the hook denies the bypass (`--no-verify`) and blocks a premature stop, which git hooks cannot see\n- 2026-09-05 the proof scripts run real harness sessions and are cited as evidence rather than folded into `cargo test`, so the Rust suite stays runnable on a machine with no agent CLI\n- 2026-09-05 re-pointed from rules-block to check-face for the same reason as guard: a red index or tree is red by G1 today; the hook does not care which rule made it so\n- 2026-09-05 the pending commit message reaches `weeder check` through `--message-file` (check-face); the claude hook can extract `-m` text from the `git commit` command it sees and hand it over, which is the only way a `Weed-allow:` trailer counts before the commit exists\n- 2026-09-05 first pleach attempt lost: the worker wrote `BLOCKED.md` (the honest path), the repo's `.gitignore` ignored that name, and pleach's scoped `git add` of an ignored path failed catastrophically, disposing the tree and the reason with it. `BLOCKED.md` is no longer ignored; the defect is reported to pleach as P7\n- 2026-09-05 the Gemini proof is a human check until the owner signs in: `~/.gemini/settings.json` carries no auth type on this machine, so no worker can run a real Gemini session here. The script is still built here; the owner runs it\n- 2026-09-05 a Claude Code session started from inside another one refuses to run while the `CLAUDECODE` environment variable is set; the proof script must start `claude` with that variable unset (`env -u CLAUDECODE claude -p ...`) and pass the Stop hook through `--settings`. tilth's benchmark runner does the same\n- 2026-09-05 a worker's dogfood notes go in `docs/dogfood/hooks.md`, not `docs/dogfood.md` (AGENTS.md); neither is tracked\n- 2026-09-05 built by pleach node `hooks` (claude/opus, codex audit) in one attempt on the retry, 28 minutes, landed through `pleach land`. `src/core/shell.rs` reads a command line the way a shell would (quotes, `&&`, pipes, `cd`, `sh -c` to eight levels) so `git commit` is found wherever it hides and left alone inside an `echo`; codex's decision table is read from the JSON schemas inside its binary and pinned by a test that reads `docs/proof-2026-09.md` back; the real Claude Code Stop run is captured there verbatim on 2.1.261\n- 2026-09-05 decided by the worker and kept: `weeder hook` refuses at exit 2 when it could not judge, never 3, because Claude Code reads a hook's exit 3 as a non-blocking error and would let the commit through; the reason goes on both stdout (the harness's JSON) and stderr. The 0/2/3 contract stays whole for `check`; the hook face is a different caller with a different protocol, and docs/proof-2026-09.md says so\n- 2026-09-05 superseded: the Tried line above about `--message-file` being how a trailer counts before the commit exists. The hooks judge with `--strict`, under which a trailer changes nothing, so the hook lifts no message off the command line; plumbing whose effect no test can observe was left out on purpose\n- 2026-09-06 agent-drafted from the run journal: failed after 0 attempts\n- 2026-09-06 the owner refuted the Gemini live proof: Google retired the personal sign-in Gemini CLI 0.46.0 uses (`IneligibleTierError`, migrate to Antigravity, which is an IDE agent with a language server and no hook surface), so the proof cannot start on this machine, and the owner's call is that the Claude Code live proof and the Codex note are enough for now. `tests/hook_gemini.rs` still proves the JSON shapes. A Gemini API key in the environment would let `scripts/proof/gemini-stop.sh` run the day it is wanted\n- 2026-09-06 c4's evidence had been passing without landing anything since the prose pass moved this section's heading from a comma to a colon: `scripts/proof/claude-stop.sh` ran a real session, asserted on the transcript, then handed a heading no line in the document matched to an awk that copied the file back unchanged and reported success. The capture in `docs/proof-2026-09.md` had been frozen at 2026-09-05 ever since. Both proof scripts now refuse before they write if the heading is not in the document, and read the dated line back after they write; the claude capture is a fresh session under the new name. An awk that matches nothing is the same shape as an awk that matched, which is why the script had to be made to say which\n\n## Rules\n- Write the failing test first where it does not exist; then make it pass.\n- You must not edit the loop file — check states belong to the verifier.\n- You must not weaken, skip, or mock the unit under test to force a green; mocked-unit evidence is detected and refused.\n- Solve the CLASS of problem the claim names, not the test's example inputs. Hardcoding fixture strings, phrases, or patterns from the check or its tests into source (lookup tables, regex lists, phrase maps) is refused work even when the test goes green — it is an answer key, not a product.\n- If the claim cannot be met generically with what you have, that is a real finding: write BLOCKED.md naming the missing capability (a model, a library, a redesign, a split of the check into honest tiers) instead of faking generality.\n- Production quality only: no stubs, no TODOs, no placeholder implementations; match the style of the surrounding code.\n- Probe/scratch files (toolchain checks, experiments) go in `.loop-scratch/` only — it is never collected; scratch files elsewhere pollute the delivery.\n- If you are genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs. An honest BLOCKED.md is a good outcome; a faked green is the worst possible one."
      },
      "accept": {
        "smoke": "tend2 verify docs/tend2/hooks.tend2.html --repo-root . --force --expect-payload ccc3a22fbfb1 --runner 'bash scripts/check/run.sh {evidence}'",
        "audit": {
          "command": "tend2 verify docs/tend2/hooks.tend2.html --repo-root . --force --expect-payload ccc3a22fbfb1 --audit-egress --runner 'bash scripts/check/run.sh {evidence}'",
          "provider": "opencode",
          "model": "deepseek/deepseek-v4-pro",
          "selfIntegrity": true
        }
      },
      "policy": {
        "timeoutMs": 5400000
      },
      "needs": [
        "check-face"
      ],
      "worker": {
        "provider": "claude",
        "model": "opus"
      }
    },
    {
      "id": "rules-block",
      "work": {
        "prompt": "You are a worker in a build loop. Make the checks below pass, honestly.\nNobody reviews your prose — only the verifier's verdict counts, so put all effort into the work itself.\n\n## The goal\nThe rules that block by default, because each names a shape no honest change has: a deleted test, an added skip or focus, a stub in production code, a secret, an edited guardrail, and a conflict marker (G1, already live on check-face). Each lands as a failing fixture first in TypeScript/JavaScript, Python, Rust and Go, then the detector, and each has a neighbour fixture where it must stay silent. A rule detects a shape, never a vendor or a product name.\n\n## Your environment\n- You are at the repository root of an isolated git worktree. Work here directly.\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- The loop file (docs/tend2/rules-block.tend2.html) is your full context — read it, including its narrative. It is READ-ONLY for you.\n\n## Checks to close\n- (code) S1's unfinished signal (`raise NotImplementedError`, `todo!()`, `unimplemented!()`, a method body that only throws) stays silent when a class or trait in the tree derives from or implements the enclosing type and defines a method of the same name in production code, an override in a test file counting for nothing, in py, rs and ts; where nothing overrides it the finding stands and its message says so (`nothing in the repository overrides it`); on a base exported through an entry file (`lib.rs`, `index.*`, a package's `__init__.py`) an unoverridden method is reported at warn with a message saying the override may live in another repository; fire and silent fixtures per language pin both, and calibration and recall run again since a block rule moved · evidence expected at: tests/rule_s1.rs\n\n## Definition of done — verify yourself before finishing\nYou are done when this exact command exits 0 (it is the gate the conductor will run):\n\n    tend2 verify docs/tend2/rules-block.tend2.html --repo-root . --force --expect-payload 3bf3b954dabb --runner 'bash scripts/check/run.sh {evidence}'\n\nRun it yourself. If it does not exit 0, you are not done. Do not declare success in prose that a command disagrees with.\n\n## Already tried (do not repeat)\n- 2026-09-05 rule files are `src/core/rules/check/<id>.rs`, one per rule, registered in `src/core/rules/check/mod.rs`; scan rules get their own registry so the two faces never share a file\n- 2026-09-05 T2 (assertions dropped) is on rules-tests although it blocks by default: its count depends on per-language assertion vocabularies that the tests loop owns, and the block set here is the one the umbrella loop names as unambiguous\n- 2026-09-05 X1's prefix list overlaps pleach's hygiene gate on purpose: both are the same shapes, and weeder is the one that runs where pleach does not\n- 2026-09-05 built by pleach node `rules-block` (claude/opus, codex audit) in one attempt, 41 minutes, landed by hand after `pleach land` met the hardening loop's change to the git seam; merged as both readings (a NUL byte is a binary blob and reads as nothing, anything else reads as text with stray bytes replaced). T1 counts cases from the reader's test shape on both sides; T3, S1 and X1 read through a syntax mask so a marker inside a string or a comment says nothing; the secret and marker fixtures use placeholders the harness expands, so the repository carries no credential and no marker\n- 2026-09-05 conductor correction, fixed inline and re-verified: the worker concluded that weeder must refuse its own hook installation under C1 (`.githooks/*` is a guardrail path) and made adoption a commit that skips the hooks, which the hooks face denies outright. C1 now recognises weeder's own bundle byte for byte (`guard::is_own_bundle`: the binary its marker names and the branches its exec line protects, handed back to `guard::script`), so adopting weeder goes through the gate the hooks run, and a bundle that differs by one byte is a guardrail edit again. `tests/rule_c1.rs` pins both halves; the guard tests adopt weeder with ordinary commits\n- 2026-09-05 left open, for calibration: `weeder check` on weeder's own tree reports the T3 and S1 fixtures, which are meant to look dishonest. A per-path specimen exclusion in `weeder.toml` is the fix; a fixture that stops carrying a skip stops being a fixture\n- 2026-09-05 cape-town on C1's self-recognition: byte for byte on weeder's own bundle is the right shape; when the stem later generates that bundle, the pin moves to the stem's output. Never widen it to a path match\n- 2026-09-06 calibration over 635 real commits (docs/calibration-2026-09.md, `Where weeder was wrong`) found three shapes worth their own checks, added above: seven of nine block-level false positives are X1 reading `name = value` in a css class attribute or a markdown sentence; S1 calls a Python `Protocol` method with an ellipsis body a stub; T1 counts only statically declared cases, so a case generated in a loop reads as one that vanished. Calibration now needs this loop, so the next run measures the fixed rules\n- 2026-09-06 cape-town's C1 ruling, after calibration showed 42 of 71 blocks were honest workflow edits: split it. Block-level C1 is the constitution tier, the agent's own guardrails, harness hook configs, `.githooks/*`, `weeder.toml`, the hard-limits sections of `AGENTS.md` and `CLAUDE.md`. GitHub workflow files are by-laws, edited legitimately all the time, and move to C3 at warn, promoted to block per repository under `[guardrails] paths`. The C1 check above is reworded to the split, which voids its stamp on purpose\n- 2026-09-06 built by pleach node `rules-block` (claude/opus, codex audit) in one attempt, 63 minutes, all ten checks stamped and landed through `pleach land` without conflict. X1's key-like-name heuristic now runs only on files the classifier calls source, with a `prose` fixture set of markup and markdown for the silence; S1 reads an ellipsis body inside a `Protocol` or abstract class as a declaration; T1 counts generated cases through a new `generated.rs` reader that follows a `for` around `it`, a `t.Run` in a range, a `parametrize` list and a macro-generated `#[test]`; C3 is the workflow rule at warn, `[guardrails] paths` in `weeder.toml` promotes a path to C1's block, and C1 keeps the constitution tier. The catalogue and `docs/rules.md` carry C3, and `xtask/tests/calibrate.rs` follows the split. Calibration runs next on these rules\n- 2026-09-06 cape-town, after calibration's second run: X1 should know the difference between a secret and a textbook. The AWS documentation key it blocked in a corpus commit is a published example credential; X1 carries a small allowlist of the examples vendors print in their own docs and reports them at note level. Shaped above. It is a list of exact published strings, not a vendor name a shape depends on, and the fixtures keep them out of the tree the way the forge token already is\n- 2026-09-06 conductor: the pleach running these nodes stages every path the Write and Edit tools touched, wherever it is. A path outside this worktree (under `$TMPDIR` or `/tmp`) makes the collector ask git about a file it cannot see, which kills the delivery and removes the worktree (dogfood P8, P11); `.loop-scratch/` is collected and judged by the hygiene gate (P10). So: never use the Write or Edit tool outside this worktree; write scratch outside it only through the shell; leave `.loop-scratch/` empty before you finish; and never leave a credential-shaped string, even a quoted one, in a file that will be collected\n- 2026-09-06 built by pleach node `rules-block` again (claude/opus, codex audit), one attempt, landed through `pleach land`. X1 carries four published examples as stamp and tail, compares a token against the halves so one character away is a credential again, and reaches the note only after both real paths found nothing on the line, so a line quoting a manual cannot hide a key. The harness keeps the same halves under `{{weeder:example-<name>}}` and `{{weeder:altered-<name>}}`, and no whole example is anywhere in the tree. Calibration runs once more on this, since the corpus commit that quoted the AWS example no longer blocks\n- 2026-09-08 owner's issue 14: six of the fourteen S1 blocks on an audited tree were the six methods of six strategy base classes, each overridden by a subclass elsewhere in the tree; the classes derive from a framework class, so the declaring-base list would not have helped. The tree already answers M1's resolution and T1's moved-case search, so the cost is one outline walk. Shaped above with the library case from the issue's open point: a base exported through an entry file states a contract for consumers outside the tree\n\n## Rules\n- Write the failing test first where it does not exist; then make it pass.\n- You must not edit the loop file — check states belong to the verifier.\n- You must not weaken, skip, or mock the unit under test to force a green; mocked-unit evidence is detected and refused.\n- Solve the CLASS of problem the claim names, not the test's example inputs. Hardcoding fixture strings, phrases, or patterns from the check or its tests into source (lookup tables, regex lists, phrase maps) is refused work even when the test goes green — it is an answer key, not a product.\n- If the claim cannot be met generically with what you have, that is a real finding: write BLOCKED.md naming the missing capability (a model, a library, a redesign, a split of the check into honest tiers) instead of faking generality.\n- Production quality only: no stubs, no TODOs, no placeholder implementations; match the style of the surrounding code.\n- Probe/scratch files (toolchain checks, experiments) go in `.loop-scratch/` only — it is never collected; scratch files elsewhere pollute the delivery.\n- If you are genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs. An honest BLOCKED.md is a good outcome; a faked green is the worst possible one."
      },
      "accept": {
        "smoke": "tend2 verify docs/tend2/rules-block.tend2.html --repo-root . --force --expect-payload 3bf3b954dabb --runner 'bash scripts/check/run.sh {evidence}'",
        "audit": {
          "command": "tend2 verify docs/tend2/rules-block.tend2.html --repo-root . --force --expect-payload 3bf3b954dabb --audit-egress --runner 'bash scripts/check/run.sh {evidence}'",
          "provider": "opencode",
          "model": "deepseek/deepseek-v4-pro",
          "selfIntegrity": true
        }
      },
      "policy": {
        "timeoutMs": 5400000
      },
      "needs": [
        "check-face"
      ],
      "worker": {
        "provider": "claude",
        "model": "opus"
      }
    },
    {
      "id": "scan",
      "work": {
        "prompt": "You are a worker in a build loop. Make the checks below pass, honestly.\nNobody reviews your prose — only the verifier's verdict counts, so put all effort into the work itself.\n\n## The goal\n`weeder scan` reads the repository as it is, not a diff, and names repo-state hygiene: a path, command, flag or symbol cited in the docs that no longer exists; a public symbol nothing references; a TODO older than the configured age; a dependency pin far behind the registry. It never blocks: exit 0 or 3 only, findings at warning level. tend2's hygiene loop is its first consumer, citing `weeder scan --format sarif` as evidence; `docs/tend2-seam.md` is the proposal for that seam.\n\n## Your environment\n- You are at the repository root of an isolated git worktree. Work here directly.\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- The loop file (docs/tend2/scan.tend2.html) is your full context — read it, including its narrative. It is READ-ONLY for you.\n\n## Checks to close\n- (code) R1 keeps the `:line` or `:start-end` a citation carries and, when the path resolves, reports a line past the end of the file under the same rule id with its own sentence naming the file's line count; a bare name in a code span resolves as a symbol at warn only where a path citation on the same line, or the nearest preceding one in the same section that resolves, anchors it, against that file's outline first and the whole tree second, with the message naming the file; an unanchored name resolves against the whole tree and is reported at note; a citation resolving to a directory anchors the names after it to everything under it and the message names the directory · evidence expected at: tests/rule_r1.rs\n\n## Definition of done — verify yourself before finishing\nYou are done when this exact command exits 0 (it is the gate the conductor will run):\n\n    tend2 verify docs/tend2/scan.tend2.html --repo-root . --force --expect-payload d339500818c5 --runner 'bash scripts/check/run.sh {evidence}'\n\nRun it yourself. If it does not exit 0, you are not done. Do not declare success in prose that a command disagrees with.\n\n## Already tried (do not repeat)\n- 2026-09-05 settled with tend2 on the channel (umbrella loop, 2026-09-05): `scan` owns repo-state hygiene and never blocks; tend2 keeps doc-evidence staleness and its hygiene loop pattern; `tend2 gate` and `weeder check` are different questions and both emit SARIF\n- 2026-09-05 R4 never reaches the network during `scan`; the snapshot is a committed file so the rule is reproducible in CI\n- 2026-09-05 cape-town: tend2 is scan's first consumer and is on pollen; before R1 to R4 are finalised, ask tend2 which SARIF fields its hygiene loop reads (asked the same day; the answer goes here, and this loop is not dispatched until it lands or tend2 says it has no preference)\n- 2026-09-05 `garden.json` lists the faces the binary answers and `tests/garden_manifest.rs` holds it equal to `weeder --help`; this loop adds `scan` to the manifest's `faces` and to `SKILL.md`, or that test fails at its own gate\n- 2026-09-05 cape-town: do not wait on tend2. Emit the minimal SARIF every consumer reads (ruleId, level, message.text, locations[0].physicalLocation.artifactLocation.uri, region.startLine) plus `properties.path` on each result for per-path aggregation; tend2's eventual answer is additive fields, never a change to those. Dispatched on that\n- 2026-09-05 built by pleach node `scan` (claude/opus, codex audit) in one attempt, 45 minutes, landed by hand after `pleach land` met the rules-tests landing in `src/core/syntax.rs` and the fixtures README. The face reads the tree as it sits and leaves with 0 or 3; R1 resolves paths against the tree and commands against their own help through a new exec seam with a five-second deadline (`[docs] commands` names which); R2 exempts entry files, `main`, dunder names and runner prefixes; R3 asks git to blame only the files that carry a marker; R4 measures against `.weeder/registry-snapshot.json` and `--refresh-snapshot` fetches through curl so the binary stays a judge. Every result carries `properties.path`. Conductor review found nothing to fix\n- 2026-09-05 `garden.json` and `SKILL.md` now name `scan`; `docs/tend2-seam.md` shows tend2's hygiene loop citing `weeder scan --format sarif` and proposes `weeder check` in place of its `mock.ts`, with `scripts/check/docs-tend2-seam.sh` running the commands it shows\n- 2026-09-06 cape-town, after milestone 5: two binding checks before calibration dispatches. R1 executes nothing outside `[docs] commands`, only with `--help`, never through a shell, never with an argument from the document, proven on a fixture with a test-owned PATH; and a plain `weeder scan` is offline, proven under a denied network, with `weeder rules` naming which rules may reach the network and how. Shaped as the two checks above\n- 2026-09-06 the two execution-bound checks landed by pleach node `scan` (claude/opus, codex audit) in one attempt, 28 minutes, through `pleach land`. The exec bound was already true and is now recorded by the commands themselves: a test-owned PATH whose programs write their own argv, two lines in the log (`listed-tool --help`, `listed-tool build --help`), the unlisted tool never called, no shell reached. The offline proof found a real defect: curl failing to resolve a host read as a registry with no latest release, so a refresh with no network wrote an empty snapshot and every pin would have read as current; a refresh now tells unreachable from unreadable, carries the committed value over for a package it could not ask about, and exits 3 leaving the snapshot untouched when it reached nothing. `weeder rules` carries a network column: `none` on every rule but R4, which says `registries under --refresh-snapshot`\n- 2026-09-06 the offline proof failed on every CI run since it landed: GitHub's Ubuntu 24.04 runners restrict unprivileged user namespaces, so `unshare --user --net` could not start and the test refused, as designed, rather than pass on nothing. CI now lifts `kernel.apparmor_restrict_unprivileged_userns` before the suite, so the proof runs there for real. The conductor had reported CI green on the strength of an older run; watching the run is part of a push\n- 2026-09-08 owner's issue 15: on an audited tree R1 produced 260 warnings, among them harness command names read as symbols, and the fifteen findings on the one document that mattered sat under 245 others; a cited `:479` in a 163-line file passed because `normalize()` cut the line before resolving. Shaped above as anchoring with a level split, which keeps every finding and changes only how loud it is; a `[docs] vocabulary` allowance list was rejected because it teaches a repository to write allowances for prose\n\n## Rules\n- Write the failing test first where it does not exist; then make it pass.\n- You must not edit the loop file — check states belong to the verifier.\n- You must not weaken, skip, or mock the unit under test to force a green; mocked-unit evidence is detected and refused.\n- Solve the CLASS of problem the claim names, not the test's example inputs. Hardcoding fixture strings, phrases, or patterns from the check or its tests into source (lookup tables, regex lists, phrase maps) is refused work even when the test goes green — it is an answer key, not a product.\n- If the claim cannot be met generically with what you have, that is a real finding: write BLOCKED.md naming the missing capability (a model, a library, a redesign, a split of the check into honest tiers) instead of faking generality.\n- Production quality only: no stubs, no TODOs, no placeholder implementations; match the style of the surrounding code.\n- Probe/scratch files (toolchain checks, experiments) go in `.loop-scratch/` only — it is never collected; scratch files elsewhere pollute the delivery.\n- If you are genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs. An honest BLOCKED.md is a good outcome; a faked green is the worst possible one."
      },
      "accept": {
        "smoke": "tend2 verify docs/tend2/scan.tend2.html --repo-root . --force --expect-payload d339500818c5 --runner 'bash scripts/check/run.sh {evidence}'",
        "audit": {
          "command": "tend2 verify docs/tend2/scan.tend2.html --repo-root . --force --expect-payload d339500818c5 --audit-egress --runner 'bash scripts/check/run.sh {evidence}'",
          "provider": "opencode",
          "model": "deepseek/deepseek-v4-pro",
          "selfIntegrity": true
        }
      },
      "policy": {
        "timeoutMs": 5400000
      },
      "needs": [
        "check-face"
      ],
      "worker": {
        "provider": "claude",
        "model": "opus"
      }
    }
  ],
  "warnings": [
    {
      "loopId": "calibration-audit",
      "code": "unwired-modules",
      "message": "10 checks cite 9 src modules (plans/cast.json, scripts/check/calibration-agreement.sh, docs/calibration-audit-2026-09.md, docs/calibration-audit-blind-2026-09.md, docs/calibration/refused/blind-2026-09-first/, docs/calibration/refused/, fixtures/adversarial/calibration-audit/, scripts/audit/blind-run.sh, scripts/audit/sighted-run.sh), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "calibration",
      "code": "unwired-modules",
      "message": "11 checks cite 6 src modules (docs/calibration-2026-09.md, garden.json, scripts/check/calibration-bar.sh, docs/calibration/corpus.toml, docs/calibration-audit-2026-09.md, docs/calibration/ruling-2026-09-06.toml), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "core",
      "code": "unwired-modules",
      "message": "5 checks cite 5 src modules (Cargo.toml, weeder.toml, AGENTS.md, CLAUDE.md, src/core/), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "garden-fit",
      "code": "unwired-modules",
      "message": "9 checks cite 15 src modules (Cargo.toml, schemas/garden.schema.json, .brand/products/weeder/identity.md, .github/workflows/ci.yml, .github/workflows/release.yml, npm/package.json, npm/, garden.json, scripts/check/calibration-bar.sh, SKILL.md, .brand/products/weeder/, examples/ci/github.yml, README.md, scripts/umbrella-brand.sh, tests/garden_manifest.rs), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "hardening",
      "code": "unwired-modules",
      "message": "5 checks cite 6 src modules (tests/determinism.rs, fixtures/adversarial/, tests/sarif_order.rs, src/core/, tests/core_hostile.rs, tests/check_hostile.rs), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "hidden-tests",
      "code": "unwired-modules",
      "message": "4 checks cite 4 src modules (tests/, fixtures/, docs/rules.md, SKILL.md), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "hooks",
      "code": "unwired-modules",
      "message": "5 checks cite 3 src modules (docs/proof-2026-09.md, scripts/proof/claude-stop.sh, scripts/proof/gemini-stop.sh), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "recall",
      "code": "unwired-modules",
      "message": "4 checks cite 2 src modules (docs/calibration-2026-09.md, docs/calibration/corpus.toml), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "rules-block",
      "code": "unwired-modules",
      "message": "12 checks cite 5 src modules (weeder.toml, AGENTS.md, CLAUDE.md, .github/workflows/, docs/rules.md), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "rules-prod",
      "code": "unwired-modules",
      "message": "8 checks cite 3 src modules (Cargo.toml, .gitignore, weeder.toml), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "sarif",
      "code": "unwired-modules",
      "message": "4 checks cite 2 src modules (schemas/sarif-schema-2.1.0.json, docs/sarif.md), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "scan",
      "code": "unwired-modules",
      "message": "9 checks cite 5 src modules (CLAUDE.md, AGENTS.md, README.md, /, Cargo.toml), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "tilth-core",
      "code": "unwired-modules",
      "message": "4 checks cite 5 src modules (.context/tilth-core, src/seams/reader.rs, Cargo.toml, src/, src/core/), but no single check's evidence spans 2+ of them"
    }
  ]
}
