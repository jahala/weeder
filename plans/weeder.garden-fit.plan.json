{
  "goal": "A static Rust binary that reads what an agent produced and refuses the growth that should not be there, deleted or weakened tests, skips, stubs, swallowed errors, secrets, guardrail edits, dependency-direction violations, in milliseconds, with zero tokens, every finding as SARIF 2.1.0. `weeder check` judges a diff and may block; `weeder scan` judges the tree and never blocks; `weeder guard` is the law in git through hooks; `weeder bite` proves a test fails without its change. For maintainers drowning in agent-authored pull requests who need a deterministic gate before they read, for teams running agents on several harnesses who want one law, and for the garden's own fleets through pleach.",
  "source": "docs/tend2#garden-fit",
  "nodes": [
    {
      "id": "garden-fit",
      "work": {
        "prompt": "You are a worker in a build loop. Make the checks below pass, honestly.\nNobody reviews your prose \u2014 only the verifier's verdict counts, so put all effort into the work itself.\n\n## The goal\nThe umbrella verifies every bed against a fit contract, F1 to F6. This loop ships what those checks read: `garden.json` at the root; a `SKILL.md` with a one-sentence description; the brand layer at `.brand/products/weeder/` with the name and accent marked flagged; a `LICENSE` (MIT, flagged until the owner confirms); a `README.md`; the release pipeline copied from tilth's (CI, multi-platform binaries, crates.io, an npm wrapper); and the GitHub Action example that is weeder's first distribution, `examples/ci/github.yml`.\n\n## Your environment\n- You are at the repository root of an isolated git worktree. Work here directly.\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- The loop file (docs/tend2/garden-fit.tend2.html) is your full context \u2014 read it, including its narrative. It is READ-ONLY for you.\n\n## Checks to close\n- (code) `garden.json` validates against the umbrella's `contracts/manifest.schema.json`, fetched at the version `petalsrc.example` names by `scripts/umbrella-brand.sh`: faces is an object of `cli`, `skill`, `hooks` (a harness name to the events weeder answers, claude `PreToolUse:Bash` and `Stop`, gemini `BeforeTool:run_shell_command` and `AfterAgent`, codex `PreToolUse` and `Stop`) and `git` (the hooks guard installs), `check` and `metric` are commands, `context` declares `upfront_tokens`, and a `coverage` block names the languages and file kinds the rules read; the vendored `schemas/garden.schema.json` is replaced by that schema at that version and `tests/garden_manifest.rs` validates against it, with every value still true of this repository \u00b7 evidence expected at: tests/garden_manifest.rs\n\n## Definition of done \u2014 verify yourself before finishing\nYou are done when this exact command exits 0 (it is the gate the conductor will run):\n\n    tend2 verify docs/tend2/garden-fit.tend2.html --repo-root . --force --expect-payload cdabbdef1e5d --runner 'bash scripts/check/run.sh {evidence}'\n\nRun it yourself. If it does not exit 0, you are not done. Do not declare success in prose that a command disagrees with.\n\n## Already tried (do not repeat)\n- 2026-09-05 the fit runner `scripts/fit/weeder.sh` and `garden.lock` on the umbrella do not exist yet; this loop ships what they will read and the umbrella loop stays open until they do\n- 2026-09-05 a landing page is a later loop; the garden footer with the plotplot band goes on the README until a page exists\n- 2026-09-05 re-pointed to need guard and hooks instead of calibration: `SKILL.md` must name every subcommand the binary prints, so it is written after the faces exist; the metric's run is calibration's check, this loop only wires its name into the manifest. This worktree merges the guard and hooks branches, which both add a subcommand to `src/main.rs`; keep both arms\n- 2026-09-05 built by pleach node `garden-fit` (claude/opus, codex audit) in one attempt, 31 minutes, landed through `pleach land` as 4d3e973. The mark was drawn twice: the worker found the owner's direction (shears cutting a weeder) in the project memory Claude Code shares across sessions of one repository, and redrew it as open shears around a stem, rendered and read at 16, 20, 28 and 48 px. `garden.json` declares the four faces the binary answers today; `tests/garden_manifest.rs` holds the manifest equal to `weeder --help`, so scan and bite must add themselves when they land. Contrast numbers in `colors.md` are recomputed by `scripts/check/brand.sh`, never trusted as written. Conductor review found nothing to fix; the layout block in AGENTS.md was the conductor's to update and is\n- 2026-09-05 the accent decision is the owner's: bramble `#8E3B5E` proposed, woad `#3D4E8C` and nettle `#6F8F1F` measured beside it; the name and MIT stay flagged with it\n- 2026-09-05 decided by the owner through cape-town, the same evening: the accent is bramble `#8E3B5E` (a word on paper at 6.57:1; a fill only on soil-night, where a bramble word lifts to `#B85C82`), the name is weeder, and the licence is MIT. The umbrella authored a brand layer (tagline \"tests still mean what they meant.\", voice signature \"pulled before it takes root.\", a bramble-cane mark) and named this repository its canonical home; adopted whole except the mark, which stays the shears the owner asked for directly, in bramble. The evidence script no longer asks for three candidates or a flag; it asks that the accent be one value everywhere and that the numbers still add up\n- 2026-09-05 open-source prep at the owner's request: FUNDING.yml, dependabot.yml (cargo, npm, github-actions), CONTRIBUTING.md, SECURITY.md and CODE_OF_CONDUCT.md in tilth's shape; a Support block and a License line on the README; the repository's description and topics set. The npm name `weeder` has been an empty placeholder since 2015 under another maintainer, so the wrapper publishes as `@plotplot/weeder` under the garden's scope with public access; the crate and the binary stay plain `weeder`, which crates.io has free\n- 2026-09-08 cape-town: `contracts/manifest.schema.json` exists on the umbrella (stem plan section 4) and the committed `garden.json` predates its shape: faces becomes an object of cli, skill, hooks and git with per-harness event lists, check and metric become objects of command and name, context_cost becomes an object with upfront_tokens, and a coverage block of languages and kinds is required. The manifest is updated when cape-town reports the schema with the hook declaration; not before, so the change is made once against the real contract\n\n## Rules\n- Write the failing test first where it does not exist; then make it pass.\n- You must not edit the loop file \u2014 check states belong to the verifier.\n- You must not weaken, skip, or mock the unit under test to force a green; mocked-unit evidence is detected and refused.\n- Solve the CLASS of problem the claim names, not the test's example inputs. Hardcoding fixture strings, phrases, or patterns from the check or its tests into source (lookup tables, regex lists, phrase maps) is refused work even when the test goes green \u2014 it is an answer key, not a product.\n- If the claim cannot be met generically with what you have, that is a real finding: write BLOCKED.md naming the missing capability (a model, a library, a redesign, a split of the check into honest tiers) instead of faking generality.\n- Production quality only: no stubs, no TODOs, no placeholder implementations; match the style of the surrounding code.\n- Scratch (probes, experiments, packets, session logs) goes under `$TMPDIR`, written through the shell only, never with the Write or Edit tool, and never under `.loop-scratch/` or anywhere in this worktree: the collector stages every path the editor touched, a path outside the worktree kills the delivery, and the hygiene gate judges whatever is left in the tree, including its secret scan.\n- If you are genuinely blocked, write BLOCKED.md at the repo root \u2014 what you tried, what stopped you, what a fix needs. An honest BLOCKED.md is a good outcome; a faked green is the worst possible one."
      },
      "accept": {
        "smoke": "tend2 verify docs/tend2/garden-fit.tend2.html --repo-root . --force --expect-payload cdabbdef1e5d --runner 'bash scripts/check/run.sh {evidence}'",
        "audit": {
          "command": "tend2 verify docs/tend2/garden-fit.tend2.html --repo-root . --force --expect-payload cdabbdef1e5d --audit-egress --runner 'bash scripts/check/run.sh {evidence}'",
          "provider": "codex",
          "selfIntegrity": true
        }
      },
      "policy": {
        "timeoutMs": 5400000
      },
      "needs": [],
      "worker": {
        "provider": "claude",
        "model": "opus"
      }
    }
  ],
  "warnings": [
    {
      "loopId": "calibration-audit",
      "code": "unwired-modules",
      "message": "10 checks cite 9 src modules (plans/cast.json, scripts/check/calibration-agreement.sh, docs/calibration-audit-2026-09.md, docs/calibration-audit-blind-2026-09.md, docs/calibration/refused/blind-2026-09-first/, docs/calibration/refused/, fixtures/adversarial/calibration-audit/, scripts/audit/blind-run.sh, scripts/audit/sighted-run.sh), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "calibration",
      "code": "unwired-modules",
      "message": "11 checks cite 6 src modules (docs/calibration-2026-09.md, garden.json, scripts/check/calibration-bar.sh, docs/calibration/corpus.toml, docs/calibration-audit-2026-09.md, docs/calibration/ruling-2026-09-06.toml), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "core",
      "code": "unwired-modules",
      "message": "5 checks cite 5 src modules (Cargo.toml, weeder.toml, AGENTS.md, CLAUDE.md, src/core/), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "garden-fit",
      "code": "unwired-modules",
      "message": "8 checks cite 15 src modules (Cargo.toml, schemas/garden.schema.json, .brand/products/weeder/identity.md, .github/workflows/ci.yml, .github/workflows/release.yml, npm/package.json, npm/, garden.json, scripts/check/calibration-bar.sh, SKILL.md, .brand/products/weeder/, examples/ci/github.yml, README.md, scripts/umbrella-brand.sh, tests/garden_manifest.rs), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "hardening",
      "code": "unwired-modules",
      "message": "5 checks cite 6 src modules (tests/determinism.rs, fixtures/adversarial/, tests/sarif_order.rs, src/core/, tests/core_hostile.rs, tests/check_hostile.rs), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "hidden-tests",
      "code": "unwired-modules",
      "message": "4 checks cite 4 src modules (tests/, fixtures/, docs/rules.md, SKILL.md), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "hooks",
      "code": "unwired-modules",
      "message": "5 checks cite 3 src modules (docs/proof-2026-09.md, scripts/proof/claude-stop.sh, scripts/proof/gemini-stop.sh), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "recall",
      "code": "unwired-modules",
      "message": "4 checks cite 2 src modules (docs/calibration-2026-09.md, docs/calibration/corpus.toml), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "rules-block",
      "code": "unwired-modules",
      "message": "12 checks cite 5 src modules (weeder.toml, AGENTS.md, CLAUDE.md, .github/workflows/, docs/rules.md), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "rules-prod",
      "code": "unwired-modules",
      "message": "8 checks cite 3 src modules (Cargo.toml, .gitignore, weeder.toml), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "sarif",
      "code": "unwired-modules",
      "message": "4 checks cite 2 src modules (schemas/sarif-schema-2.1.0.json, docs/sarif.md), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "scan",
      "code": "unwired-modules",
      "message": "9 checks cite 5 src modules (CLAUDE.md, AGENTS.md, README.md, /, Cargo.toml), but no single check's evidence spans 2+ of them"
    },
    {
      "loopId": "tilth-core",
      "code": "unwired-modules",
      "message": "4 checks cite 5 src modules (.context/tilth-core, src/seams/reader.rs, Cargo.toml, src/, src/core/), but no single check's evidence spans 2+ of them"
    }
  ]
}
