# calibration audit blind case packet, 2026-09

Generated by `cargo xtask audit-packet --dir` from the pinned corpus. This file is the auditor session's only input for this case. It contains machine-derived case identity, rule catalogue text, weeder's findings as rule id, level, path and line, a line count per file the change touched, and verbatim git diff bytes: the whole change where it fits the stated cap, and the files the findings name where it does not.

Seed: calibration-audit-blind-2026-09-reseal-1
Case: recall:T4:ts:pleach:3a1306011:test/loop/run-work.test.ts:10
Diff cap bytes: 75000
Diff bytes: 499
Diff capped: no
Diff shown: the whole change

Rule: T4

Catalogue:

```text
T4  warn   check  none                                 A tolerance or a timeout was widened
```

Weeder findings:

| Rule | Level | Path | Line |
|---|---|---|---:|
| D1 | warning | `package.json` | 40 |

Files this change touched:

| Path | Lines added | Lines deleted |
|---|---:|---:|
| `test/loop/run-work.test.ts` | 1 | 1 |

Planted site: test/loop/run-work.test.ts:10

Planted shape: a wait was widened from 1000 to 10000

Question: is rule T4's shape genuinely present at that planted site, and did weeder report it there?

```diff
diff --git a/test/loop/run-work.test.ts b/test/loop/run-work.test.ts
index 8fd49c5..4cced0c 100644
--- a/test/loop/run-work.test.ts
+++ b/test/loop/run-work.test.ts
@@ -7,7 +7,7 @@ import { promptFor, runWork } from '../../src/loop/run-work.ts';
 // spec: §6 run-work — the three Work shapes and their gates. In-memory Worker
 // and scripted ExecFn; the subject is runWork's control flow.
 
-const TIMEOUT = 1000;
+const TIMEOUT = 10000;
 
 function node(over: Partial<Node>): Node {
   return {
```
