# calibration audit blind case packet, 2026-09

Generated by `cargo xtask audit-packet --dir` from the pinned corpus. This file is the auditor session's only input for this case. It contains machine-derived case identity, rule catalogue text, weed findings, a line count per file the change touched, and verbatim git diff bytes: the whole change where it fits the stated cap, and the files the findings name where it does not.

Seed: calibration-audit-blind-2026-09-redo-2
Case: recall:D2:go:hcl:bd45ab812:hclwrite/format.go:6
Diff cap bytes: 75000
Diff bytes: 350
Diff capped: no
Diff shown: the whole change

Rule: D2

Catalogue:

```text
D2  block  check  none                                 An import crossed a forbidden boundary
```

Weed findings:

| Rule | Level | Path | Line | Message |
|---|---|---|---:|---|
| C3 | warning | `.github/workflows/checks.yml` | 0 | a workflow was changed. this file decides which checks a server runs, so a change here can quietly stop running one. read the diff against what the workflow used to run, and say in the change why a step moved. |

Files this change touched:

| Path | Lines added | Lines deleted |
|---|---:|---:|
| `hclwrite/format.go` | 1 | 0 |

Planted site: hclwrite/format.go:6-8

Planted shape: `hclwrite` was made to import `integrationtest`

Question: is rule D2's shape genuinely present at that planted site, and did weed report it there?

```diff
diff --git a/hclwrite/format.go b/hclwrite/format.go
index a9d8029..c6455d7 100644
--- a/hclwrite/format.go
+++ b/hclwrite/format.go
@@ -5,6 +5,7 @@ package hclwrite
 
 import (
 	"github.com/hashicorp/hcl/v2/hclsyntax"
+	"github.com/hashicorp/hcl/v2/integrationtest"
 )
 
 // format rewrites tokens within the given sequence, in-place, to adjust the
```
