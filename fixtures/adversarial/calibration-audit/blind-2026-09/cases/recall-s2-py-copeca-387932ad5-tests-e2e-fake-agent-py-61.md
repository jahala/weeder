# calibration audit blind case packet, 2026-09

Generated by `cargo xtask audit-packet --dir` from the pinned corpus. This file is the auditor session's only input for this case. It contains machine-derived case identity, rule catalogue text, weeder's findings as rule id, level, path and line, a line count per file the change touched, and verbatim git diff bytes: the whole change where it fits the stated cap, and the files the findings name where it does not.

Seed: calibration-audit-blind-2026-09-reseal-1
Case: recall:S2:py:copeca:387932ad5:tests/e2e/fake_agent.py:61
Diff cap bytes: 75000
Diff bytes: 532
Diff capped: no
Diff shown: the whole change

Rule: S2

Catalogue:

```text
S2  warn   check  none                                 An error was swallowed
```

Weeder findings:

| Rule | Level | Path | Line |
|---|---|---|---:|
| none | none |  | 0 |

Files this change touched:

| Path | Lines added | Lines deleted |
|---|---:|---:|
| `tests/e2e/fake_agent.py` | 4 | 1 |

Planted site: tests/e2e/fake_agent.py:61

Planted shape: a handler was added that catches and says nothing

Question: is rule S2's shape genuinely present at that planted site, and did weeder report it there?

```diff
diff --git a/tests/e2e/fake_agent.py b/tests/e2e/fake_agent.py
index 4055fb2..49306f8 100644
--- a/tests/e2e/fake_agent.py
+++ b/tests/e2e/fake_agent.py
@@ -56,7 +56,10 @@ def _extract_prompt(argv: list[str]) -> str:
     the last argument so the agent is still usable under other invocations.
     """
     if "--" in argv:
-        idx = argv.index("--")
+        try:
+            idx = argv.index("--")
+        except Exception:
+            pass
         return " ".join(argv[idx + 1 :])
     return argv[-1] if argv else ""
 
```
