# calibration audit blind case packet, 2026-09

Generated by `cargo xtask audit-packet --dir` from the pinned corpus. This file is the auditor session's only input for this case. It contains machine-derived case identity, rule catalogue text, weed findings, a line count per file the change touched, and verbatim git diff bytes: the whole change where it fits the stated cap, and the files the findings name where it does not.

Seed: calibration-audit-blind-2026-09-t2-1
Case: recall:T1:ts:tend2:6d9a1cf91:test/site-paths.test.ts
Diff cap bytes: 75000
Diff bytes: 948
Diff capped: no
Diff shown: the whole change

Rule: T1

Catalogue:

```text
T1  block  check  none                                 A test was deleted
```

Weed findings:

| Rule | Level | Path | Line | Message |
|---|---|---|---:|---|
| T2 | error | `test/site-paths.test.ts` | 26 | 3 assertions went out of this file: it made 9 and now makes 6. a case that checks nothing passes whatever the code does, so the suite reports green over behaviour held by nobody. put the checks back, or carry a `Weed-allow: T2` trailer saying which claim stopped being worth making. |

Files this change touched:

| Path | Lines added | Lines deleted |
|---|---:|---:|
| `test/site-paths.test.ts` | 0 | 11 |

Planted site: test/site-paths.test.ts

Planted shape: the case `${dir} (${pages.length} pages)` was deleted

Question: is rule T1's shape genuinely present at that planted site, and did weed report it there?

```diff
diff --git a/test/site-paths.test.ts b/test/site-paths.test.ts
index d76aa2a..6b836fa 100644
--- a/test/site-paths.test.ts
+++ b/test/site-paths.test.ts
@@ -26,17 +26,6 @@ function assetRefs(html: string): string[] {
 describe('every page\'s renderer assets resolve at their exact relative path', () => {
   for (const dir of PAGE_DIRS) {
     const pages = readdirSync(join(root, dir)).filter((f) => f.endsWith('.tend2.html'));
-    it(`${dir} (${pages.length} pages)`, () => {
-      expect(pages.length).toBeGreaterThan(0);
-      for (const page of pages) {
-        const html = readFileSync(join(root, dir, page), 'utf8');
-        const refs = assetRefs(html);
-        expect(refs.length, `${dir}/${page} references no renderer assets`).toBeGreaterThan(0);
-        for (const ref of refs) {
-          expect(existsSync(join(root, dir, ref)), `${dir}/${page} → ${ref} does not exist`).toBe(true);
-        }
-      }
-    });
   }
 });
 
```
