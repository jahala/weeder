# calibration audit blind case packet, 2026-09

Generated by `cargo xtask audit-packet --dir` from the pinned corpus. This file is the auditor session's only input for this case. It contains machine-derived case identity, rule catalogue text, weed findings, a line count per file the change touched, and verbatim git diff bytes: the whole change where it fits the stated cap, and the files the findings name where it does not.

Seed: calibration-audit-blind-2026-09-t2-1
Case: recall:T6:ts:tend2:ad070a0d4:test/verify.test.ts:176
Diff cap bytes: 75000
Diff bytes: 665
Diff capped: no
Diff shown: the whole change

Rule: T6

Catalogue:

```text
T6  warn   check  none                                 An error assertion was weakened
```

Weed findings:

| Rule | Level | Path | Line | Message |
|---|---|---|---:|---|
| S3 | warning | `src/cli/index.ts` | 1153 | production code was added that prints for a person to read: `console.log`. a library writing to a stream it does not own is output the caller never asked for. take the line out, or report it through whatever this repository logs with. |
| S3 | warning | `src/cli/index.ts` | 1156 | production code was added that prints for a person to read: `console.log`. a library writing to a stream it does not own is output the caller never asked for. take the line out, or report it through whatever this repository logs with. |
| G2 | warning | `src/run/casting.ts` | 0 | a file of 17164 bytes was added whose bytes are not text. nobody can review what changed in it, and no later change to it can be read either. keep it out of the repository, or say where it came from and how it is rebuilt. |

Files this change touched:

| Path | Lines added | Lines deleted |
|---|---:|---:|
| `test/verify.test.ts` | 1 | 1 |

Planted site: test/verify.test.ts:176

Planted shape: an error assertion stopped naming the error

Question: is rule T6's shape genuinely present at that planted site, and did weed report it there?

```diff
diff --git a/test/verify.test.ts b/test/verify.test.ts
index 1cee077..fd10574 100644
--- a/test/verify.test.ts
+++ b/test/verify.test.ts
@@ -173,7 +173,7 @@ describe('human checks', () => {
 describe('shape + judge', () => {
   it('descriptions refuse verification', () => {
     const file = writeLoop('persona', '# p — persona\n**Who.** someone\n\n## Jobs\n- When x, I want y, so I can z.\n');
-    expect(() => runVerify(file, { repoRoot: repo, runner: RUNNER })).toThrow(/description/);
+    expect(() => runVerify(file, { repoRoot: repo, runner: RUNNER })).toThrow();
   });
 
   it('an agent check is stamped only through an explicit judge verdict', () => {
```
