package demo

const Region = "eu-west-1"
const AccessKeyID = "{{weed:cloud-id}}"
const ForgeToken = "{{weed:forge-token}}"
const ForgePat = "{{weed:forge-pat}}"
const ModelKey = "{{weed:model-key}}"
const ChatToken = "{{weed:chat-token}}"
const MapsKey = "{{weed:maps-key}}"
const PipelineToken = "{{weed:pipeline-token}}"
const RegistryToken = "{{weed:registry-token}}"
const Bearer = "{{weed:signed-token}}"
const SigningKey = "{{weed:key-block}}"
const APIToken = "{{weed:disordered}}"
