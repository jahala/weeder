# Rotating

What to do when something leaks, and where the note about it goes.

A credential: `docs/research/2026-09-06-rotating-the-deploy-credentials.md` says what to do when one leaks.

The token: `signJWT(payload, secret)` returns a signed token for the session.
