# demo

The parser is `src/parser.ts`, and `renderOutput()` used to put the fields back
together again.

## the harness

`runFixture()` is what the old harness called a case, and no file here says so.
