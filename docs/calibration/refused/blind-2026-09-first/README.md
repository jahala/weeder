# A refused blind audit, kept as a specimen

The first blind re-grade, 2026-09-06, codex, seed `calibration-audit-blind-2026-09-codex`.
It came back forty of forty and was refused unlanded: the packet's `diff` blocks are not
diffs but sentences the same worker wrote after reading the ledger, so the auditor was
handed the builder's reasoning in a code fence. `audit.md` and `transcript.md` are the
files as the node left them. Nothing under `docs/calibration/refused/` is read by
`scripts/check/calibration-agreement.sh`; this is what a false blind looks like.
