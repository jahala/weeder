# calibration audit blind case packet, 2026-09

Generated by `cargo xtask audit-packet --dir` from the pinned corpus. This file is the auditor session's only input for this case. It contains machine-derived case identity, rule catalogue text, weed findings and verbatim git diff bytes when the diff fits the stated cap.

Seed: calibration-audit-blind-2026-09-fair-1
Case: recall:T6:ts:tend2:56072cdab:test/verify.test.ts:176
Diff cap bytes: 75000
Diff bytes: 667
Diff capped: no

Rule: T6

Catalogue:

```text
T6  warn   check  none                                 An error assertion was weakened
```

Weed findings:

| Rule | Level | Path | Line | Message |
|---|---|---|---:|---|
| S3 | warning | `src/cli/index.ts` | 1061 | production code was added that prints for a person to read: `console.log`. a library writing to a stream it does not own is output the caller never asked for. take the line out, or report it through whatever this repository logs with. |
| S3 | warning | `src/cli/index.ts` | 1062 | production code was added that prints for a person to read: `console.log`. a library writing to a stream it does not own is output the caller never asked for. take the line out, or report it through whatever this repository logs with. |
| S3 | warning | `src/cli/index.ts` | 1063 | production code was added that prints for a person to read: `console.log`. a library writing to a stream it does not own is output the caller never asked for. take the line out, or report it through whatever this repository logs with. |
| S3 | warning | `src/cli/index.ts` | 1064 | production code was added that prints for a person to read: `console.log`. a library writing to a stream it does not own is output the caller never asked for. take the line out, or report it through whatever this repository logs with. |
| S2 | warning | `src/serve/api.ts` | 104 | a `catch` was added whose block does nothing with the failure. the program learns the call went wrong and carries on as though it had not. log the failure, wrap it for the caller, hand it on, or say in a comment why it is right to drop it. |
| S2 | warning | `src/serve/api.ts` | 224 | a `catch` was added whose block does nothing with the failure. the program learns the call went wrong and carries on as though it had not. log the failure, wrap it for the caller, hand it on, or say in a comment why it is right to drop it. |
| S2 | warning | `src/serve/client.ts` | 51 | a `catch` was added whose block does nothing with the failure. the program learns the call went wrong and carries on as though it had not. log the failure, wrap it for the caller, hand it on, or say in a comment why it is right to drop it. |
| S2 | warning | `src/serve/protocol.ts` | 96 | a `catch` was added whose block does nothing with the failure. the program learns the call went wrong and carries on as though it had not. log the failure, wrap it for the caller, hand it on, or say in a comment why it is right to drop it. |

Planted site: test/verify.test.ts:176

Planted shape: an error assertion stopped naming the error

Question: is rule T6's shape genuinely present at that planted site, and did weed report it there?

```diff
diff --git a/test/verify.test.ts b/test/verify.test.ts
index 1cee077a..fd105742 100644
--- a/test/verify.test.ts
+++ b/test/verify.test.ts
@@ -173,7 +173,7 @@ describe('human checks', () => {
 describe('shape + judge', () => {
   it('descriptions refuse verification', () => {
     const file = writeLoop('persona', '# p — persona\n**Who.** someone\n\n## Jobs\n- When x, I want y, so I can z.\n');
-    expect(() => runVerify(file, { repoRoot: repo, runner: RUNNER })).toThrow(/description/);
+    expect(() => runVerify(file, { repoRoot: repo, runner: RUNNER })).toThrow();
   });
 
   it('an agent check is stamped only through an explicit judge verdict', () => {
```
