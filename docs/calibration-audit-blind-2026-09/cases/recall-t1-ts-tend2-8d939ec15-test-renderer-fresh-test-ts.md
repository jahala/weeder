# calibration audit blind case packet, 2026-09

Generated by `cargo xtask audit-packet --dir` from the pinned corpus. This file is the auditor session's only input for this case. It contains machine-derived case identity, rule catalogue text, weed findings and verbatim git diff bytes when the diff fits the stated cap.

Seed: calibration-audit-blind-2026-09-fair-1
Case: recall:T1:ts:tend2:8d939ec15:test/renderer-fresh.test.ts
Diff cap bytes: 75000
Diff bytes: 568
Diff capped: no

Rule: T1

Catalogue:

```text
T1  block  check  none                                 A test was deleted
```

Weed findings:

| Rule | Level | Path | Line | Message |
|---|---|---|---:|---|
| T2 | error | `test/renderer-fresh.test.ts` | 27 | 2 assertions went out of this file: it made 4 and now makes 2. a case that checks nothing passes whatever the code does, so the suite reports green over behaviour nobody is holding. put the checks back, or carry a `Weed-allow: T2` trailer saying which claim stopped being worth making. |

Planted site: test/renderer-fresh.test.ts

Planted shape: the case `it` was deleted

Question: is rule T1's shape genuinely present at that planted site, and did weed report it there?

```diff
diff --git a/test/renderer-fresh.test.ts b/test/renderer-fresh.test.ts
index db01e43b..f78e887d 100644
--- a/test/renderer-fresh.test.ts
+++ b/test/renderer-fresh.test.ts
@@ -27,9 +27,5 @@ describe('the built assets derive from src/render and self-declare', () => {
 
 describe('every committed sibling copy is byte-fresh against the build', () => {
   for (const dir of SIBLING_DIRS) {
-    it(dir, () => {
-      expect(read(join(dir, 'loop.css'))).toBe(read('dist/loop.css'));
-      expect(read(join(dir, 'loop.js'))).toBe(read('dist/loop.js'));
-    });
   }
 });
```
