# calibration audit blind case packet, 2026-09

Generated by `cargo xtask audit-packet --dir` from the pinned corpus. This file is the auditor session's only input for this case. It contains machine-derived case identity, rule catalogue text, weed findings and verbatim git diff bytes when the diff fits the stated cap.

Seed: calibration-audit-blind-2026-09-fair-1
Case: blocked:tend2:08529d5f56
Diff cap bytes: 75000
Diff bytes: 4092883
Diff capped: yes

Rules: T1

Catalogue:

```text
T1  block  check  none                                 A test was deleted
```

Weed findings:

| Rule | Level | Path | Line | Message |
|---|---|---|---:|---|
| T1 | error | `src/adapters/claude-tasks/generator.test.ts` | 0 | a test file was deleted, and 70 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/adapters/claude-tasks/status-sync.test.ts` | 0 | a test file was deleted, and 30 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/adapters/github-issues/generator.test.ts` | 0 | a test file was deleted, and 62 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/adapters/makefile/generator.test.ts` | 0 | a test file was deleted, and 45 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/adapters/markdown/generator.test.ts` | 0 | a test file was deleted, and 60 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/adapters/render-verification.test.ts` | 0 | a test file was deleted, and 17 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/adapters/umbel/cross-repo.test.ts` | 0 | a test file was deleted, and 11 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/adapters/umbel/generator.test.ts` | 0 | a test file was deleted, and 27 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/bin/tend.test.ts` | 0 | a test file was deleted, and 12 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/cli/audit.test.ts` | 0 | a test file was deleted, and 2 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/cli/doctor.test.ts` | 0 | a test file was deleted, and 9 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/cli/execute.test.ts` | 0 | a test file was deleted, and 8 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/cli/init.test.ts` | 0 | a test file was deleted, and 55 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/cli/negctrl.test.ts` | 0 | a test file was deleted, and 10 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/cli/next.test.ts` | 0 | a test file was deleted, and 4 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/cli/preview.test.ts` | 0 | a test file was deleted, and 18 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/cli/rename-feature.test.ts` | 0 | a test file was deleted, and 22 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/cli/rename.test.ts` | 0 | a test file was deleted, and 16 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/cli/render.test.ts` | 0 | a test file was deleted, and 15 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/cli/sync.test.ts` | 0 | a test file was deleted, and 24 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/adapters.test.ts` | 0 | a test file was deleted, and 20 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/audit-result.test.ts` | 0 | a test file was deleted, and 19 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/bridge/audit-compute.test.ts` | 0 | a test file was deleted, and 10 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/bridge/emit.test.ts` | 0 | a test file was deleted, and 5 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/bridge/ingester.test.ts` | 0 | a test file was deleted, and 16 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/bridge/plan.test.ts` | 0 | a test file was deleted, and 12 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/catalog-resolve.test.ts` | 0 | a test file was deleted, and 13 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/constraints.test.ts` | 0 | a test file was deleted, and 155 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/coverage.test.ts` | 0 | a test file was deleted, and 23 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/diagram/diagram.test.ts` | 0 | a test file was deleted, and 22 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/execute.test.ts` | 0 | a test file was deleted, and 40 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/feature-merge.test.ts` | 0 | a test file was deleted, and 20 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/files.test.ts` | 0 | a test file was deleted, and 46 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/graph.test.ts` | 0 | a test file was deleted, and 32 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/judgment-warnings.test.ts` | 0 | a test file was deleted, and 10 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/layering.test.ts` | 0 | a test file was deleted, and 3 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/mock-detection.test.ts` | 0 | a test file was deleted, and 19 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/narrative-cites.test.ts` | 0 | a test file was deleted, and 19 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/narrative-media-orphans.test.ts` | 0 | a test file was deleted, and 26 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/next-action.test.ts` | 0 | a test file was deleted, and 31 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/progress.test.ts` | 0 | a test file was deleted, and 27 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/project-health.test.ts` | 0 | a test file was deleted, and 70 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/reason-codes.test.ts` | 0 | a test file was deleted, and 11 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/schema.test.ts` | 0 | a test file was deleted, and 115 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/side-effects.test.ts` | 0 | a test file was deleted, and 211 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/source-dirs.test.ts` | 0 | a test file was deleted, and 10 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/staleness.test.ts` | 0 | a test file was deleted, and 35 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/step-traces.test.ts` | 0 | a test file was deleted, and 11 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/svg-preview.test.ts` | 0 | a test file was deleted, and 20 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/tolerant-read.test.ts` | 0 | a test file was deleted, and 3 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/ui-html.test.ts` | 0 | a test file was deleted, and 18 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/core/write-queue.test.ts` | 0 | a test file was deleted, and 5 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/mcp/server.test.ts` | 0 | a test file was deleted, and 11 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/mcp/tools/aggregate.test.ts` | 0 | a test file was deleted, and 1 test case went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/mcp/tools/next-action.test.ts` | 0 | a test file was deleted, and 3 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/mcp/tools/output-execution-plan.test.ts` | 0 | a test file was deleted, and 20 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/mcp/tools/tools.test.ts` | 0 | a test file was deleted, and 133 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/mcp/tools/validation.integration.test.ts` | 0 | a test file was deleted, and 44 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/mcp/tools/validation.test.ts` | 0 | a test file was deleted, and 41 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/ui/components/CopyButton.test.tsx` | 0 | a test file was deleted, and 4 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/ui/components/DetailPanel.test.tsx` | 0 | a test file was deleted, and 58 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/ui/components/FeatureEdge.test.tsx` | 0 | a test file was deleted, and 7 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/ui/components/FeatureNode.test.tsx` | 0 | a test file was deleted, and 14 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/ui/components/JourneySwimlane.test.tsx` | 0 | a test file was deleted, and 16 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/ui/hooks/useFeatures.test.ts` | 0 | a test file was deleted, and 2 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/ui/hooks/useKeyboardNavigation.test.ts` | 0 | a test file was deleted, and 19 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/ui/lib/graph-builder.test.ts` | 0 | a test file was deleted, and 18 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `src/ui/lib/narrative-markdown.test.ts` | 0 | a test file was deleted, and 13 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `tests/bash/builtins.sh` | 0 | a test file was deleted, and 0 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `tests/bash/custom-verb-fallback.sh` | 0 | a test file was deleted, and 0 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `tests/bash/fixtures/builtins-fixture.tend.html` | 0 | a test file was deleted, and 0 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `tests/bash/fixtures/fallback-fixture.tend.html` | 0 | a test file was deleted, and 0 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `tests/bash/fixtures/run-fixture.tend.html` | 0 | a test file was deleted, and 0 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `tests/bash/fixtures/whitespace-marker-fixture.tend.html` | 0 | a test file was deleted, and 0 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `tests/bash/run-verb.sh` | 0 | a test file was deleted, and 0 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `tests/bash/whitespace-marker.sh` | 0 | a test file was deleted, and 0 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `tests/e2e/dogfood-render.spec.ts` | 0 | a test file was deleted, and 3 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `tests/e2e/mcp-stdio.spec.ts` | 0 | a test file was deleted, and 2 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `tests/e2e/oo-render.spec.ts` | 0 | a test file was deleted, and 284 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `tests/e2e/traceability.spec.ts` | 0 | a test file was deleted, and 4 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `tests/fixtures/audit-emit/non-discriminating/docs/tend/features/__fixture__.tend.html` | 0 | a test file was deleted, and 0 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `tests/fixtures/audit-emit/non-discriminating/unit.mjs` | 0 | a test file was deleted, and 0 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `tests/fixtures/audit-emit/non-discriminating/unit.test.mjs` | 0 | a test file was deleted, and 1 test case went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `tests/fixtures/audit-emit/passing/docs/tend/features/__fixture__.tend.html` | 0 | a test file was deleted, and 0 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `tests/fixtures/audit-emit/passing/unit.mjs` | 0 | a test file was deleted, and 0 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `tests/fixtures/audit-emit/passing/unit.test.mjs` | 0 | a test file was deleted, and 1 test case went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `tests/fixtures/audit-emit/trivial-recipe/docs/tend/features/__fixture__.tend.html` | 0 | a test file was deleted, and 0 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `tests/scripts/assembly.spec.ts` | 0 | a test file was deleted, and 13 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `tests/scripts/lib-color-tokens.spec.ts` | 0 | a test file was deleted, and 7 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `tests/scripts/narrative-links.spec.ts` | 0 | a test file was deleted, and 1 test case went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `tests/scripts/oo-style-tokens.spec.ts` | 0 | a test file was deleted, and 10 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `tests/scripts/shell-core.spec.ts` | 0 | a test file was deleted, and 3 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `tests/skills/skill-conductor.spec.ts` | 0 | a test file was deleted, and 25 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |
| T1 | error | `tests/skills/skill-vocabulary.spec.ts` | 0 | a test file was deleted, and 5 test cases went with it. a suite that is gone reports nothing, so whatever it covered is now covered by nobody. restore the file, or move its cases into the file that takes its place. |

```text
The full git diff for this case exceeds the stated cap, so the generator omitted it instead of cutting it silently.
```
